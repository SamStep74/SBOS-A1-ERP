# Dependencies
node_modules/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
evals/karpathy/results/

# Worktrees and orchestration scratch
.claude/worktrees/
.orchestration/*/[a-z]*/
!.orchestration/README.md

# Build output
dist/
build/
coverage/

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Env / secrets
.env
.env.*
!.env.example

# TypeScript build info
*.tsbuildinfo
.sbos.db

# Backup artifacts (Wave 51+)
# $SBOS_BACKUP_DIR — runtime DB snapshots + validate uploads.
backups/

# Runtime artifacts (Wave 65+)
# $SBOS_EMAILS_CAPTURE_DIR — email service capture mode writes here.
# Tests + smoke runs produce *.jsonl files that should never be
# committed (they're test output, not source).
var/
