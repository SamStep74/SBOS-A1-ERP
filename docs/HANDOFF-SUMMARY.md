<!-- Mirrored from A1-ERP-HY @ 50f5f44d632f8a3112ae5579060b768f0028c5da on 2026-06-16 -->
# Armosphera One Claude — Handoff & State

_Last updated: 2026-06-07 · Catalog quote margin-rule evidence · 51 tags · **full suite verified**_

> **Repo home:** private GitHub `SamStep74/A1-Suite-Local`, developed locally at `~/dev/A1-Suite-Local` (moved off the OneDrive-synced folder — the old `node --test` "cancelled" stalls were OneDrive FS contention, now gone: the full suite runs clean on local disk).

A **sovereign, self-hostable Armenian business operating system** with phased one-to-one *functional* parity to Zoho One. Runs entirely on the customer's own server: a single Node/Fastify + SQLite process serving a React SPA, with **no external data dependency** except opt-in AI. Built for Armenian organizations that cannot use foreign clouds (government, banks, healthcare, legal).

Latest implementation slice: CRM quote lines now persist and expose the matched catalog margin-rule evidence, not only the final margin status. `quote_lines` stores `margin_rule_code`, `margin_rule_minimum_percent`, and `margin_rule_target_percent` with startup repair for existing tenant databases. Catalog-resolved quote lines carry `SERVICE-MIN-35`, `HARDWARE-MIN-25`, minimum percent, and target percent through quote reads; manual overrides and free-text lines keep blank/null rule evidence. The CRM quote evidence chips now show the matched rule code and minimum percent, so a quantity-5 scanner quote surfaces `STANDARD-SALES`, `4,250 AMD discount`, `HARDWARE-MIN-25 min 25%`, and `below minimum`. Quote pricing, public quote reads, workflow reads, finance draft-invoice creation, inventory valuation, purchase costing, POS, and portal flows remain otherwise unchanged. Verification from `~/dev/A1-Suite-Local`: focused catalog regression passes (`node --test --test-concurrency=4 --test-timeout=60000 test/catalog.test.js`, 7 pass, 0 fail); focused migration regression passes (`node --test --test-concurrency=4 --test-timeout=60000 --test-name-pattern="existing quote line tables receive catalog columns" test/api.test.js`, 1 pass, 0 fail); `git diff --check` passes; `npm run build:ui` passes with the existing Vite large-chunk warning; browser proof at `http://127.0.0.1:4198/app/crm` shows `HARDWARE-MIN-25 min 25%` and `below minimum` on desktop and mobile with no overflow; fresh smoke passes with 12 apps (`ARMOSPHERA_ONE_DB=/tmp/a1-suite-catalog-quote-margin-rule-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke`); full `node --test --test-concurrency=4 --test-timeout=120000` passes (804 pass, 0 fail, 0 cancelled).

---

## 1. What exists today

### Nine UI domains on one shared data graph — a closed revenue/procurement loop
```
Forms (intake) ─▶ CRM (lead→deal→quote) ─▶ Projects (deliver, staffed by People-HR,
   ▲                                          logging billable time)
   │                                              │
 Desk (support) ◀── Docs & Sign (execute) ◀── Finance ◀─ billing seam (time→invoice→ledger)
                         ▲                         ▲
                         └──── Catalog & Inventory ◀── Purchase ───▶ Finance/AP
```
Every arrow is a **validated FK between modules** sharing `customers` / `deals` / `people_employees` — integration depth, not copy-paste.

| Domain | Status | Key capability |
|---|---|---|
| **CRM** | complete (BE+UI) | leads, deals, quotes Tube, activity timeline |
| **Finance** | complete (BE+UI) | double-entry ledger on RA chart of accounts; AR/AP/expenses/payroll/VAT/statements/opening-balances; history lists |
| **Desk** | complete (BE+UI) | helpdesk: create/list/transition/reassign, escalation governance |
| **People-HR** | complete (BE+UI) | employee registry (ՀՎՀՀ/salary) → payroll → ledger |
| **Docs & Sign** | complete (BE+UI) | document + multi-signer e-signature lifecycle, SHA-256 consent, local-only signers, printable Save-as-PDF evidence certificate |
| **Projects** | complete (BE+UI) | projects→tasks→milestones→time entries; lazy detail expander |
| **Catalog & Inventory** | complete (BE+UI) | product master, governed stock locations/quants/moves, sidebar stock operation workspace |
| **Purchase** | complete first slice (BE+UI) | RFQ→PO confirmation→partial/full stock receipts→AP vendor bill with period-lock, vendor/pricelist defaults, receipt evidence, backup evidence, and sidebar workspace controls |
| **Forms** | complete (BE+UI) | intake forms; PUBLIC submit → creates a CRM lead (rate-limited, key-whitelisted) |

### Cross-app seams (the "suite, not a folder of apps" proof)
- **Forms → CRM**: public form submission creates a real CRM lead via `createCrmLead`.
- **People-HR → Finance**: an employee's salary runs payroll → posts `Dt 714 / Kt 521+525` to the ledger.
- **Projects → Finance (billing seam)**: unbilled logged minutes → a posted invoice (`Dt 221 / Kt 611+524`), entries marked billed (idempotent per project+period).
- **Purchase → Inventory → Finance/AP**: confirmed PO receipts create per-receipt stock moves into `WH/STOCK`; only fully received POs convert into vendor bills through the existing AP engine and period-lock checks.

### Hardening (production-readiness pass — 224 slices)
1. **Effective-dated tax-rate versioning** (`tax_rates` table; recomputing a historical period uses the rate that applied *then*).
2. **Auth/MFA rate-limiting** (per-IP + per-email login throttle, MFA attempt cap → 429).
3. **UI error surfacing** (all 20 mutation handlers surface server errors in a dismissable banner; previously silent).
4. **Finance history lists** (expenses / bills / payroll-runs were postable but unviewable — now listed).
5. **Finance RBAC** (`requireFinanceOperator` 403-gates ledger-write endpoints).
6. **Project detail expander** (lazy task/milestone/time tree).
7. **Production readiness gate** (`/api/compliance/production-readiness`) blocks production use until legal sources have Accountant/Lawyer review and effective-dated VAT/payroll rates are configured.
8. **Professional source signoff** adds a Lawyer demo role, preserves reviewer-role metadata, and requires Accountant review for tax/VAT sources plus Lawyer review for personal-data/e-sign sources before the readiness gate can pass.
9. **Downstream professional source enforcement** applies the same Accountant/Lawyer source-signoff rule to Copilot proposed actions and downstream SRC/e-sign/privacy packet creation instead of trusting `legal_sources.status = active` alone.
10. **Copilot advisory audit trail** records every legal/accounting Copilot answer as metadata-only `copilot.advisory.generated` suite and audit events, using a SHA-256 question hash instead of storing raw prompt/answer text in durable logs.
11. **Copilot citation review evidence** renders each cited Armenian legal/accounting source with professional-review status, reviewer role/name, and latest review timestamp directly in the Copilot source list.
12. **Copilot citation source links** renders safe HTTP(S) links to the maintained Armenian source URL with a visible host label, fixes reviewed-date display to use `latestReview.createdAt`, and unit-tests malformed/non-HTTP link rejection.
13. **Legal source host stability** prevents legal-source review records from moving seeded Armenian sources to arbitrary hosts while still allowing same-host path/query/version updates.
14. **Legal source host block audit** records rejected cross-host review attempts as metadata-only `legal.source.review.blocked` suite/audit events without storing the raw rejected URL or mutating source history.
15. **Legal source HTTPS downgrade guard** blocks same-host HTTPS-to-HTTP source review updates and records only source id, trusted existing host, attempted-host hash/match, protocols, and `scheme-downgrade` reason.
16. **Legal source credentialed URL guard** rejects source review URLs with username/password userinfo before mutation, records only trusted existing-host/protocol plus attempted-host hash/match metadata with `url-credentials`, and prevents credentialed legacy citation URLs from rendering as links.
17. **Audit reader gate and legal review note metadata** limits `/api/audit` to Owner/Admin/Auditor, keeps non-reader UI flows from fetching it, and stores only legal-review note hash/length in suite/audit metadata while preserving the canonical review note.
18. **A1 Platform tenant resolution bridge** optionally resolves Studio tenant context from A1 Platform via `x-a1-request-host`, keeps health public metadata minimal, gates detailed tenant summaries to audit readers, enforces egress/strict/disabled-tenant rules, and blocks cross-host session replay when Platform supplies an org mapping.
19. **Tenant-bound public forms and quotes** scopes anonymous public form pages/submissions and quote read/acceptance tokens by the resolved A1 Platform tenant org, returning generic `404` responses for wrong-host or unmapped-host access so public callers cannot distinguish foreign tenant resources from missing resources.
20. **Authenticated unmapped tenant fail-closed** rejects authenticated Studio API requests when A1 Platform resolves a tenant but does not provide a local organization mapping, while preserving local/single-tenant behavior and non-strict `tenant:null` fail-open behavior.
21. **Unmapped tenant login fail-closed** applies the same resolved-tenant org mapping guard to password login and MFA verification before challenges are created, marked verified, or sessions/cookies are issued.
22. **Platform auth failure fail-closed** treats A1 Platform auth/config failures as blocking even outside strict mode, so a bad Platform token cannot silently downgrade tenant enforcement to local fail-open mode.
23. **Platform tenant auth-cache hardening** treats Platform `401`/`403` responses as auth failures unless they carry a known blocking tenant-state code, keeps malformed/auth-proxy responses sanitized, and scopes cached tenant decisions by strict-mode and Platform-token context so token rotation or strict-mode changes cannot reuse stale allow/null decisions.
24. **Platform public-resource lookup-failure hardening** hides tenant-bound anonymous forms and public quotes behind generic `404` responses when Platform tenant lookup has a non-blocking temporary failure, while keeping `/api/health` and authenticated local continuity fail-open outside strict mode.
25. **Public evidence attribution hardening** keeps anonymous form-submitted CRM leads, suite events, and audit rows from being falsely attributed to the human Owner, and records direct socket IP evidence for public quote acceptance before considering proxy headers.
26. **Public form/session edge hardening** revokes bearer-authenticated sessions on `/api/logout`, retires stale password-only privileged sessions once MFA is active, rate-limits anonymous public form-page lookup before DB/render work, and keeps that lookup throttle effective for loopback traffic from tunnels/reverse proxies.
27. **Trusted proxy public client hardening** adds explicit opt-in public client IP resolution for trusted tunnels/reverse proxies, ignores forwarded headers by default, rejects malformed or multi-value `x-forwarded-for` into a non-exempt trusted-proxy fallback bucket, and uses the resolved client identity for auth/public rate limits plus public form/quote evidence.
28. **Public loopback throttle hardening** applies non-loopback-exempt throttles to anonymous public form submits and public quote read/accept APIs, preserves local login/setup loopback behavior, gives test workflow buyers distinct public IPs, and extends document signature consent evidence to the same explicit trusted-proxy client identity policy.
29. **Platform tenant-null public-resource hardening** keeps authenticated `tenant:null` continuity fail-open outside strict mode, but treats successful Platform `tenant:null` responses as unmapped for anonymous public forms and public quote read/accept routes so misrouted hosts receive generic `404` without reading or mutating local tenant resources.
30. **Webhook credentialed URL hardening** rejects outbound webhook endpoint URLs containing username/password userinfo before persistence, keeps the raw credentialed URL out of error bodies, and proves the endpoint table/list never stores the rejected target.
31. **Evidence payload reader hardening** makes SRC exports, signature evidence packets, privacy export packets, and privacy retention assessments expose full payload/checksum/source-key data only to the matching review roles, while general authenticated suite/list reads receive stable redacted summaries.
32. **Event feed payload reader hardening** makes `/api/events`, event-returning mutation responses, and `/api/suite` redact sensitive timeline payload keys for non-audit roles while preserving full owner/admin/auditor review evidence.
33. **Forms submission reader hardening** gates authenticated form definition/submission reads to campaign-enabled roles plus read-only Auditor, preventing Support from reading private intake submissions while preserving public submit behavior.
34. **Integration connector credential guard** rejects connector endpoint URLs containing username/password userinfo before persistence, keeps credentialed URLs and secrets out of error/list responses, and proves connector records are not written on rejection.
35. **Dashboard launcher role contract** keeps every seeded role's `/api/suite` app list aligned with the `/app/<id>` route allowlist and `suite-app-<id>` workspace anchors, with rendered desktop/mobile proof that each exposed sidebar product opens and lands on its panel.
36. **App-assignment role guard** rejects Owner app-assignment writes to unknown roles, prevents legacy invalid assignment rows from authorizing themselves, filters invalid roles from live assignment inventory, and reports enabled stale roles as `invalidAssignmentRoles` in access-review evidence instead of silently trusting or hiding them.
37. **App-assignment enabled-value guard** rejects non-boolean assignment toggles before mutation, so string values like `"false"` cannot be treated as truthy access grants and rejected writes produce no assignment audit event.
38. **Webhook enabled-value guard** preserves explicit disabled webhook endpoints while rejecting non-boolean endpoint toggles before persistence, so string values like `"false"` cannot silently create active outbound endpoints or audit records.
39. **Integration connector owner-role guard** rejects connector configuration writes with unknown owner roles before mutation, keeping Integration Hub readiness and remediation ownership evidence tied to real tenant roles.
40. **Integration connector enum guard** rejects invalid connector `status` and `environment` values before mutation, preventing silent fallback writes that could still rotate secrets, scopes, or endpoint URLs.
41. **Integration connector scope guard** rejects malformed connector scopes before mutation, preventing silent fallback writes that could still rotate endpoint URLs, environments, or secret fingerprints.
42. **Integration connector evidence text guard** rejects non-string or control-character connector notes and health-check sample evidence before mutation, preventing object coercion or multiline control text from becoming readiness/audit evidence.
43. **Integration connector secret guard** rejects non-string, over-4096-character, or control-character submitted connector secrets before hashing, preventing object coercion or malformed tokens from rotating credential fingerprints.
44. **Integration connector endpoint URL guard** rejects non-string submitted connector endpoint URLs before URL validation, preventing array/object coercion from changing connector routing targets or credential fingerprints.
45. **Integration connector enum type guard** rejects non-string submitted connector `status` and `environment` values before enum validation, preventing array coercion from changing readiness state, environment, routing, or credential fingerprints.
46. **Integration connector legacy array guard** sanitizes malformed stored connector scope evidence before list and health-check output, pins immutable capability/required-scope contracts to connector definitions, and downgrades stale stored `ready` health when current scopes miss definition-required grants.
47. **Webhook and event payload guard** rejects non-string webhook `name`, `url`, and `secret` values plus unsupported/mixed event arrays before persistence, requires suite-event payloads to be plain objects, redacts legacy array payloads to `{}`, and normalizes event-feed limits.
48. **Integration connector scalar contract guard** pins stored connector identity/provider/boundary scalar fields to definitions, sanitizes stored connector status/environment drift, and blocks stale `ready` health when current connector state is not connected.
49. **Suite event metadata guard** rejects non-string suite-event metadata before persistence, preventing object coercion in event type, subject, customer, or status evidence and keeping failed writes out of events and audit trails.
50. **Workflow test-event metadata guard** rejects malformed workflow test-event request bodies, metadata, notes, and array payloads before persistence, preventing object coercion and malformed payload evidence from entering workflow, suite-event, or audit records.
51. **Workflow dry-run metadata guard** rejects malformed workflow dry-run request bodies, customer/invoice metadata, and notes before persistence, preventing object coercion and malformed preview evidence from entering dry-run, suite-event, or audit records.
52. **Workflow rule change metadata guard** rejects malformed workflow rule state and rollback request bodies, reasons, and rollback version selectors before persistence, preventing object coercion or numeric coercion from entering rule-version, suite-event, or audit evidence.
53. **Workflow approval decision metadata guard** rejects malformed workflow approval decision request bodies, decision values, and notes before persistence, preventing array/object coercion from approving actions or entering suite-event/audit evidence.
54. **Finance period close metadata guard** rejects malformed finance period close request bodies and close reasons before persistence, preventing array/object/control-character evidence from entering period locks, suite events, or audit trails.
55. **Draft invoice posting metadata guard** rejects malformed draft invoice post request bodies and official invoice numbers before persistence, preventing object/array/control-character invoice evidence from entering receivables, links, ledger, suite events, or audit trails.
56. **Payment receipt metadata guard** rejects malformed payment receipt request bodies, amounts, dates, methods, and references before persistence, preventing object/array/control-character payment evidence from entering payments, invoices, ledger, suite events, audit trails, collection fulfillment, or webhooks.
57. **Bank transaction import metadata guard** rejects malformed Armenian bank import request bodies, amounts, dates, directions, and text fields before persistence, preventing object/array/control-character bank evidence from entering bank transactions, source keys, match evidence, suite events, audit trails, or downstream reconciliation.
58. **SRC export metadata guard** rejects malformed SRC export request bodies, period keys, and notes before persistence, preventing object/array/control-character export evidence from entering SRC packets, source keys, suite events, or audit trails.
59. **Signature packet metadata guard** rejects malformed accepted-quote signature packet request bodies, quote IDs, and notes before persistence, preventing object/array/control-character evidence from entering Docs signature packets, source keys, suite events, or audit trails.
60. **Finance expense metadata guard** rejects malformed expense posting request bodies, amounts, dates, descriptions, and vendors before persistence, preventing object/array/control-character evidence from entering expenses, ledger journal memos, or audit trails.
61. **Finance bill metadata guard** rejects malformed supplier bill request bodies, amounts, dates, suppliers, and descriptions before persistence, preventing object/array/control-character evidence from entering bills, ledger journal memos, or audit trails.
62. **Finance bill payment metadata guard** rejects malformed bill payment request bodies, amounts, dates, methods, and references before persistence, preventing object/array/control-character evidence from entering bill payments, payable status, ledger journal entries, or audit trails.
63. **Finance opening balance metadata guard** rejects malformed opening-balance request bodies, dates, entries, balance-sheet account codes, and amounts before persistence, preventing object/array/control-character evidence from changing opening balances, ledger journal rows, or audit trails.
64. **Payroll run metadata guard** rejects malformed finance and People-HR payroll run request bodies, gross amounts, dates, employee evidence, and payroll config overrides before persistence, preventing object/array/control-character evidence from entering payroll runs, ledger journal rows, or audit trails.
65. **People-HR employee metadata and payroll config bounds guard** rejects malformed employee create/update request bodies, names, tax IDs, salaries, dates, status values, and contact fields before persistence, while also bounding payroll override rates, thresholds, and stamp brackets so impossible config evidence cannot enter payroll runs, employee registry rows, or audit trails.
66. **Payroll preview metadata guard** rejects malformed payroll calculation preview request bodies, gross amounts, effective dates, and bounded config overrides before returning calculations, preventing invalid preview evidence while preserving no-persistence preview behavior.
67. **Legal source review metadata guard** rejects malformed professional legal-source review request bodies, titles, URLs, effective dates, statuses, review notes, non-default URL ports, and overlong source URLs before persistence, and keeps rejected host labels out of block metadata while preserving hash/match evidence.
68. **Admin evidence metadata guard** rejects malformed audit-export, access-review, and tenant-backup request bodies, notes, period bounds, and review periods before persistence, preventing object/array/control-character evidence from entering admin packets or audit trails.
69. **Docs & Sign metadata guard** rejects malformed document, signer, signing, and void request bodies, titles, document bodies, document types, customer/signer IDs, signer contact metadata, and void reasons before persistence, preventing object/array/control-character evidence from entering document rows, signer rows, signature state, suite events, or audit trails.
70. **Project metadata guard** rejects malformed project, task, milestone, and time-entry request bodies, text, IDs, dates, booleans, statuses, and whole-minute values before persistence, preventing object/array/control-character evidence from entering delivery tracking rows or audit trails.
71. **Forms metadata guard and Docs send-body follow-up** rejects malformed form definition/public submission request bodies and non-empty document-send bodies before persistence, preventing object/array/control-character evidence from entering forms, submissions, CRM leads, public pages, Docs send state, or audit trails.
72. **CRM lead metadata guard** rejects malformed lead creation request bodies, company/contact/email/phone/tax/source/channel/interest/consent fields, and unsafe estimated values before persistence, preventing object/array/control-character evidence from entering CRM leads, suite events, or audit trails.
73. **CRM lead conversion metadata guard** rejects malformed lead conversion request bodies, forecast categories, deal titles, and next-step text before persistence, preventing object/array/control-character evidence from creating customers, profile sources, deals, conversion activities, suite events, or audit trails.
74. **CRM deal forecast metadata guard and conversion default-title bound** rejects malformed deal forecast request bodies, categories, dates, and manager notes before persistence, preserves existing forecast rows on rejected updates, and keeps generated lead-conversion deal titles within the valid title limit for max-length company names.
75. **CRM quote metadata guard** rejects malformed quote creation and quote-release approval request bodies, IDs, titles, dates, line descriptions, quantities, prices, and approval notes before persistence, preventing object/array/control-character evidence from entering quotes, quote lines, workflow approvals, suite events, or audit trails.
76. **Public quote acceptance metadata guard and quote-handoff note prevalidation** rejects malformed public quote acceptance request bodies, signer names/emails, optional acceptance dates, and internal pilot quote-handoff approval notes before persistence, preventing object/array/control-character evidence from accepting quotes, winning deals, creating finance approvals, sending webhooks, preserving unsafe repeat-acceptance evidence, or orphaning pilot quote drafts.
77. **Collection promise and reminder metadata guard** rejects malformed CRM collection promise and reminder request bodies, promised amounts/dates, reminder channels, notes, and provider evidence before persistence, preventing object/array/control-character evidence from scheduling payment promises, moving CRM tasks, sending reminders, or writing suite/audit records.
78. **Legal question metadata guard** rejects malformed legal-question request bodies, customer IDs, topics, and question text before persistence, preventing object/array/control-character evidence from creating legal questions, answers, citations, approvals, suite events, or audit records.
79. **AI advisory metadata guard** rejects malformed customer-brief, deal-risk, overdue-invoice, ticket-summary, and workflow-builder request bodies, record IDs, and prompts before persistence, preventing object/array/control-character evidence from creating advisory rows, suite events, or audit records.
80. **Copilot question metadata guard** rejects malformed Copilot request bodies, questions, intents, customer/document/employee IDs, period/as-of dates, and payroll gross preview inputs before advisory generation, preventing object/array/control-character evidence from creating Copilot timeline or audit events.
81. **Service case metadata guard** rejects malformed Desk service-case, reply, update, escalation, and resolution request bodies before persistence, preventing object/array/control-character evidence from entering service cases, messages, escalation/resolution rows, suite events, or audit records while preserving normal multiline service narratives for replies, escalations, and resolution summaries.
82. **Project billing metadata guard** rejects malformed project bill-time request bodies, hourly rates, issue dates, period keys, and due-day values before invoice, ledger, billed-entry, or audit persistence.
83. **Docs template metadata guard** rejects malformed document-template generate request bodies, customer IDs, variable keys, and variable values before generated document or audit persistence.
84. **Analytics metadata guard** rejects malformed semantic snapshot and report packet request bodies, report dates, period keys, report selectors, formats, and notes before analytics snapshot/report, suite-event, or audit persistence.
85. **Privacy request metadata guard** rejects malformed personal-data request bodies, customer IDs, request types, requester emails, channels, and notes before privacy request, workflow approval, suite-event, or audit persistence.
86. **App assignment metadata guard** rejects malformed app-assignment request bodies, role values, and enabled toggles before app assignment or audit persistence.
87. **Session revoke metadata guard** rejects malformed admin session-revoke request bodies and reasons before session revocation or audit persistence.
88. **Suite route alias guard** normalizes legacy `/app/hayhashvapah` to the canonical Finance app and collapses the Forms alias onto Campaigns before app assignment fallback, preventing stale product links from landing on the wrong dashboard.
89. **Dashboard sidebar openability guard** adds stable suite launcher button metadata and a Playwright-backed seeded-role matrix test proving every assigned sidebar product opens its canonical route and workspace panel.
90. **Suite route decode guard** treats malformed percent-encoded `/app/<id>` path segments as unknown app ids instead of throwing during SPA route selection, preserving the default CRM fallback on bad local links.
91. **Suite route assignment fallback guard** prevents stale invalid assigned app ids from becoming selected dashboard routes by falling back only to canonical suite ids or known aliases, with CRM as the last-resort local workspace.
92. **Suite route canonicalization guard** trims and lowercases route app ids before alias and allow-list matching, so copied links like `/app/Finance` or encoded `/app/%20copilot%20` open the intended product instead of falling through to a different assigned app.
93. **Suite route generation guard** makes `appRoute()` emit only canonical suite app paths, so direct route generation for aliases, whitespace/case drift, or unknown ids resolves to Finance/Copilot/CRM-safe URLs instead of preserving malformed path segments.
94. **MFA enrollment metadata guard** rejects non-object privileged MFA enrollment bodies and non-string labels before pending factor creation, preventing object/null coercion from entering MFA factor labels or audit-backed enrollment evidence.
95. **MFA verification metadata guard** rejects non-object enrollment-verification bodies and non-string factor/code values before factor lookup or failed-verification audit, preventing coerced object evidence from entering MFA verification trails.
96. **Project billing preview query guard** validates preview hourly-rate and as-of query metadata with the same project billing normalizers used by persisted billing, rejecting malformed preview inputs before quote/VAT calculations.
97. **Legal law-search query guard** validates Armenian legal search query text and result-limit metadata before RAG lookup, rejecting malformed query values instead of coercing objects, control text, overlong prompts, or unsafe limits into search behavior.
98. **Pilot template install body guard** rejects non-object clinic/wellness pilot install bodies before template installation, suite-event, audit, or backup evidence writes while preserving omitted-body demo defaults.
99. **Analytics as-of query guard** validates analytics receivables-aging, semantic-metrics, and metric-drilldown as-of query dates before calculation, rejecting malformed dates instead of falling back to the default reporting date.
100. **Analytics snapshot filter query guard** validates semantic snapshot `metricId`, `reportDate`, `from`, and `to` filters before reading snapshot rows, rejecting malformed filter metadata instead of coercing unsafe query values.
101. **CRM list query filter guard** validates CRM lead `status` and quote `customerId` list filters before reading CRM rows, rejecting malformed filter metadata instead of coercing unsafe query values.
102. **Collection list query filter guard** validates collection promise and reminder `customerId` list filters before reading collection rows, rejecting malformed filter metadata instead of coercing unsafe query values.
103. **Signature packet list query filter guard** validates Docs signature-packet `customerId` list filters before reading signature evidence rows, rejecting malformed filter metadata instead of coercing unsafe query values.
104. **Finance list query filter guard** validates Finance draft-invoice, payment, and bank-transaction `customerId` list filters before reading finance rows, rejecting malformed filter metadata instead of coercing unsafe query values.
105. **Legal/privacy list query filter guard** validates legal-question and privacy-request `customerId` list filters before reading legal/privacy rows, rejecting malformed filter metadata instead of coercing unsafe query values.
106. **Workflow list query filter guard** validates workflow approval `status` and workflow dry-run/test-event/run `customerId` list filters before reading workflow rows, rejecting malformed filter metadata instead of coercing unsafe query values.
107. **Clinic launch-clearance list query filter guard** validates clinic/wellness launch-clearance `remediationPlanId` filters before reading pilot clearance rows, rejecting malformed filter metadata instead of coercing unsafe query values.
108. **Clinic paid-offer list query filter guard** validates clinic/wellness paid-offer `clearancePacketId` filters before reading pilot offer rows, rejecting malformed filter metadata instead of coercing unsafe query values.
109. **Clinic quote-handoff list query filter guard** validates clinic/wellness quote-handoff `offerId` filters before reading pilot quote-handoff rows, rejecting malformed filter metadata instead of coercing unsafe query values.
110. **Clinic quote-release list query filter guard** validates clinic/wellness quote-release `quoteHandoffId` filters before reading pilot quote-release rows, rejecting malformed filter metadata instead of coercing unsafe query values.
111. **Clinic quote-acceptance-handoff list query filter guard** validates clinic/wellness quote-acceptance-handoff `quoteReleasePacketId` filters before reading pilot quote-acceptance-handoff rows, rejecting malformed filter metadata instead of coercing unsafe query values.
112. **Clinic HayHashvapah draft list query filter guard** validates clinic/wellness HayHashvapah draft `acceptanceHandoffId` filters before reading pilot draft-invoice packet rows, rejecting malformed filter metadata instead of coercing unsafe query values.
113. **Clinic official-invoice list query filter guard** validates clinic/wellness official-invoice `draftPacketId` filters before reading pilot invoice-posting packet rows, rejecting malformed filter metadata instead of coercing unsafe query values.
114. **Clinic payment/closeout list query filter guard** validates clinic/wellness payment-collection, closeout, and renewal-chain list query filters before reading pilot packet rows, rejecting malformed packet metadata instead of coercing unsafe query values.
115. **Production-readiness query filter guard** validates production-readiness `asOf` filters before evaluating legal/accounting readiness gates, rejecting malformed date metadata instead of falling back to the current date.
116. **Finance report query filter guard** validates VAT report `period` and payables report `asOf` filters before reading ledger/accounting reports, rejecting malformed report metadata instead of coercing unsafe query values.
117. **Event feed limit query filter guard** validates suite event feed `limit` filters before reading event timeline rows, rejecting malformed pagination metadata instead of silently falling back to the default limit.
118. **Integration connector key guard** validates integration connector path keys before configure and health-check mutations, rejecting malformed key metadata instead of treating unsafe keys as missing connectors.
119. **Webhook delivery id guard** validates webhook delivery retry path IDs before delivery lookup and retry work, rejecting malformed delivery metadata instead of treating unsafe IDs as missing deliveries.
120. **Analytics path id guard** validates semantic metric and analytics report path IDs before drilldown/report lookups, rejecting malformed analytics metadata instead of treating unsafe IDs as missing analytics records.
121. **Public form id guard** validates public and authenticated form path IDs before page rendering, public submission, detail reads, or updates, rejecting malformed form metadata instead of treating unsafe IDs as missing forms.
122. **Public quote token guard** validates public quote read and acceptance tokens before quote lookup or acceptance work, rejecting malformed public token metadata instead of treating unsafe tokens as missing quotes.
123. **Document template id guard** validates document-template generation path IDs before template lookup and document creation, rejecting malformed template metadata instead of treating unsafe IDs as missing templates.
124. **Clinic remediation action key guard** validates clinic/wellness remediation action path keys before resolution lookups, rejecting malformed action metadata instead of treating unsafe keys as missing remediation actions.
125. **Clinic quote handoff offer id guard** validates clinic/wellness paid-offer path IDs before quote handoff creation, rejecting malformed offer metadata instead of treating unsafe IDs as missing paid offers.
126. **Clinic quote release handoff id guard** validates clinic/wellness quote-handoff path IDs before quote-release packet creation, rejecting malformed handoff metadata instead of treating unsafe IDs as missing quote handoffs.
127. **Clinic quote acceptance release id guard** validates clinic/wellness quote-release path IDs before quote-acceptance handoff creation, rejecting malformed release metadata instead of treating unsafe IDs as missing quote releases.
128. **Clinic draft invoice acceptance handoff id guard** validates clinic/wellness quote-acceptance handoff path IDs before HayHashvapah draft-invoice packet creation, rejecting malformed handoff metadata instead of treating unsafe IDs as missing acceptance handoffs.
129. **Clinic official invoice draft packet id guard** validates clinic/wellness HayHashvapah draft-invoice packet path IDs before official invoice posting packet creation, rejecting malformed draft packet metadata instead of treating unsafe IDs as missing draft packets.
130. **Clinic payment collection posting packet id guard** validates clinic/wellness official-invoice posting packet path IDs before payment-collection packet creation, rejecting malformed posting packet metadata instead of treating unsafe IDs as missing posting packets.
131. **Clinic closeout payment collection id guard** validates clinic/wellness payment-collection packet path IDs before closeout packet creation, rejecting malformed payment-collection metadata instead of treating unsafe IDs as missing payment-collection packets.
132. **Clinic renewal quote closeout id guard** validates clinic/wellness closeout packet path IDs before renewal quote handoff creation, rejecting malformed closeout metadata instead of treating unsafe IDs as missing closeout packets.
133. **Clinic renewal release handoff id guard** validates clinic/wellness renewal quote handoff path IDs before renewal quote release packet creation, rejecting malformed renewal handoff metadata instead of treating unsafe IDs as missing renewal handoffs.
134. **Clinic renewal acceptance release id guard** validates clinic/wellness renewal quote release packet path IDs before renewal quote acceptance handoff creation, rejecting malformed renewal release metadata instead of treating unsafe IDs as missing renewal release packets.
135. **Clinic renewal draft acceptance handoff id guard** validates clinic/wellness renewal quote acceptance handoff path IDs before HayHashvapah draft-invoice packet creation, rejecting malformed renewal acceptance handoff metadata instead of treating unsafe IDs as missing renewal acceptance handoffs.
136. **Clinic renewal official invoice draft packet id guard** validates clinic/wellness renewal HayHashvapah draft-invoice packet path IDs before official invoice posting packet creation, rejecting malformed renewal draft packet metadata instead of treating unsafe IDs as missing renewal draft packets.
137. **Clinic renewal payment collection posting packet id guard** validates clinic/wellness renewal official-invoice posting packet path IDs before payment-collection packet creation, rejecting malformed renewal posting packet metadata instead of treating unsafe IDs as missing renewal posting packets.
138. **Clinic renewal closeout payment collection id guard** validates clinic/wellness renewal payment-collection packet path IDs before renewal closeout packet creation, rejecting malformed renewal payment-collection metadata instead of treating unsafe IDs as missing renewal payment-collection packets.
139. **Clinic next renewal quote closeout id guard** validates clinic/wellness renewal closeout packet path IDs before next-renewal quote handoff creation, rejecting malformed renewal closeout metadata instead of treating unsafe IDs as missing renewal closeout packets.
140. **Clinic next renewal release handoff id guard** validates clinic/wellness next-renewal quote handoff path IDs before next-renewal quote release packet creation, rejecting malformed next-renewal handoff metadata instead of treating unsafe IDs as missing next-renewal handoffs.
141. **Clinic next renewal acceptance release id guard** validates clinic/wellness next-renewal quote release packet path IDs before next-renewal quote acceptance handoff creation, rejecting malformed next-renewal release metadata instead of treating unsafe IDs as missing next-renewal release packets.
142. **Clinic next renewal draft acceptance handoff id guard** validates clinic/wellness next-renewal quote acceptance handoff path IDs before next-renewal HayHashvapah draft-invoice packet creation, rejecting malformed next-renewal acceptance metadata instead of treating unsafe IDs as missing next-renewal acceptance handoffs.
143. **Clinic next renewal official invoice draft packet id guard** validates clinic/wellness next-renewal HayHashvapah draft-invoice packet path IDs before next-renewal official invoice posting packet creation, rejecting malformed next-renewal draft packet metadata instead of treating unsafe IDs as missing next-renewal draft packets.
144. **Clinic next renewal payment collection posting packet id guard** validates clinic/wellness next-renewal official invoice posting packet path IDs before next-renewal payment collection packet creation, rejecting malformed next-renewal posting packet metadata instead of treating unsafe IDs as missing next-renewal posting packets.
145. **Clinic next renewal closeout payment collection id guard** validates clinic/wellness next-renewal payment collection packet path IDs before next-renewal closeout packet creation, rejecting malformed next-renewal payment collection metadata instead of treating unsafe IDs as missing next-renewal payment collection packets.
146. **Clinic following renewal quote closeout id guard** validates clinic/wellness next-renewal closeout packet path IDs before following-renewal quote handoff creation, rejecting malformed next-renewal closeout metadata instead of treating unsafe IDs as missing next-renewal closeout packets.
147. **Clinic following renewal release handoff id guard** validates clinic/wellness following-renewal quote handoff path IDs before following-renewal quote release packet creation, rejecting malformed following-renewal handoff metadata instead of treating unsafe IDs as missing following-renewal handoffs.
148. **Clinic following renewal acceptance release id guard** validates clinic/wellness following-renewal quote release packet path IDs before following-renewal quote acceptance handoff creation, rejecting malformed following-renewal release metadata instead of treating unsafe IDs as missing following-renewal release packets.
149. **Clinic following renewal draft acceptance handoff id guard** validates clinic/wellness following-renewal quote acceptance handoff path IDs before following-renewal HayHashvapah draft-invoice packet creation, rejecting malformed following-renewal acceptance metadata instead of treating unsafe IDs as missing following-renewal acceptance handoffs.
150. **Clinic following renewal official invoice draft packet id guard** validates clinic/wellness following-renewal HayHashvapah draft-invoice packet path IDs before following-renewal official invoice posting packet creation, rejecting malformed following-renewal draft packet metadata instead of treating unsafe IDs as missing following-renewal draft packets.
151. **Clinic following renewal payment collection posting packet id guard** validates clinic/wellness following-renewal official-invoice posting packet path IDs before following-renewal payment-collection packet creation, rejecting malformed following-renewal posting packet metadata instead of treating unsafe IDs as missing following-renewal posting packets.
152. **Clinic following renewal closeout payment collection id guard** validates clinic/wellness following-renewal payment-collection packet path IDs before following-renewal closeout packet creation, rejecting malformed following-renewal payment-collection metadata instead of treating unsafe IDs as missing following-renewal payment-collection packets.
153. **Clinic subsequent renewal quote closeout id guard** validates clinic/wellness following-renewal closeout packet path IDs before subsequent-renewal quote handoff creation, rejecting malformed following-renewal closeout metadata instead of treating unsafe IDs as missing following-renewal closeout packets.
154. **Clinic subsequent renewal release handoff id guard** validates clinic/wellness subsequent-renewal quote handoff path IDs before subsequent-renewal quote release packet creation, rejecting malformed subsequent-renewal handoff metadata instead of treating unsafe IDs as missing subsequent-renewal handoffs.
155. **Clinic subsequent renewal acceptance release id guard** validates clinic/wellness subsequent-renewal quote release packet path IDs before subsequent-renewal quote acceptance handoff creation, rejecting malformed subsequent-renewal release metadata instead of treating unsafe IDs as missing subsequent-renewal release packets.
156. **Clinic subsequent renewal draft acceptance handoff id guard** validates clinic/wellness subsequent-renewal quote acceptance handoff path IDs before subsequent-renewal HayHashvapah draft-invoice packet creation, rejecting malformed subsequent-renewal acceptance metadata instead of treating unsafe IDs as missing subsequent-renewal acceptance handoffs.
157. **Clinic subsequent renewal official invoice draft packet id guard** validates clinic/wellness subsequent-renewal HayHashvapah draft-invoice packet path IDs before subsequent-renewal official invoice posting packet creation, rejecting malformed subsequent-renewal draft packet metadata instead of treating unsafe IDs as missing subsequent-renewal draft packets.
158. **Clinic subsequent renewal payment collection posting packet id guard** validates clinic/wellness subsequent-renewal official-invoice posting packet path IDs before subsequent-renewal payment-collection packet creation, rejecting malformed subsequent-renewal posting packet metadata instead of treating unsafe IDs as missing subsequent-renewal posting packets.
159. **Clinic subsequent renewal closeout payment collection id guard** validates clinic/wellness subsequent-renewal payment-collection packet path IDs before subsequent-renewal closeout packet creation, rejecting malformed subsequent-renewal payment-collection metadata instead of treating unsafe IDs as missing subsequent-renewal payment-collection packets.
160. **Clinic continuation renewal quote subsequent closeout id guard** validates clinic/wellness subsequent-renewal closeout packet path IDs before continuation-renewal quote handoff creation, rejecting malformed subsequent-renewal closeout metadata instead of treating unsafe IDs as missing subsequent-renewal closeout packets.
161. **Clinic continuation renewal release handoff id guard** validates clinic/wellness continuation-renewal quote handoff path IDs before continuation-renewal quote release packet creation, rejecting malformed continuation-renewal handoff metadata instead of treating unsafe IDs as missing continuation-renewal quote handoffs.
162. **Clinic continuation renewal acceptance release id guard** validates clinic/wellness continuation-renewal quote release packet path IDs before continuation-renewal quote acceptance handoff creation, rejecting malformed continuation-renewal release metadata instead of treating unsafe IDs as missing continuation-renewal release packets.
163. **Clinic continuation renewal draft acceptance handoff id guard** validates clinic/wellness continuation-renewal quote acceptance handoff path IDs before continuation-renewal HayHashvapah draft-invoice packet creation, rejecting malformed continuation-renewal acceptance metadata instead of treating unsafe IDs as missing continuation-renewal acceptance handoffs.
164. **Clinic continuation renewal official invoice draft packet id guard** validates clinic/wellness continuation-renewal HayHashvapah draft-invoice packet path IDs before continuation-renewal official invoice posting packet creation, rejecting malformed continuation-renewal draft packet metadata instead of treating unsafe IDs as missing continuation-renewal draft packets.
165. **Clinic continuation renewal payment collection posting packet id guard** validates clinic/wellness continuation-renewal official-invoice posting packet path IDs before continuation-renewal payment-collection packet creation, rejecting malformed continuation-renewal posting packet metadata instead of treating unsafe IDs as missing continuation-renewal posting packets.
166. **Clinic continuation renewal closeout payment collection id guard** validates clinic/wellness continuation-renewal payment-collection packet path IDs before continuation-renewal closeout packet creation, rejecting malformed continuation-renewal payment-collection metadata instead of treating unsafe IDs as missing continuation-renewal payment-collection packets.
167. **Clinic ongoing renewal quote continuation closeout id guard** validates clinic/wellness continuation-renewal closeout packet path IDs before ongoing-renewal quote handoff creation, rejecting malformed continuation-renewal closeout metadata instead of treating unsafe IDs as missing continuation-renewal closeout packets.
168. **Clinic ongoing renewal release handoff id guard** validates clinic/wellness ongoing-renewal quote handoff path IDs before ongoing-renewal quote release packet creation, rejecting malformed ongoing-renewal handoff metadata instead of treating unsafe IDs as missing ongoing-renewal quote handoffs.
169. **Clinic ongoing renewal acceptance release id guard** validates clinic/wellness ongoing-renewal quote release packet path IDs before ongoing-renewal quote acceptance handoff creation, rejecting malformed ongoing-renewal release metadata instead of treating unsafe IDs as missing ongoing-renewal release packets.
170. **Clinic ongoing renewal draft acceptance handoff id guard** validates clinic/wellness ongoing-renewal quote acceptance handoff path IDs before ongoing-renewal HayHashvapah draft-invoice packet creation, rejecting malformed ongoing-renewal acceptance metadata instead of treating unsafe IDs as missing ongoing-renewal acceptance handoffs.
171. **Clinic ongoing renewal official invoice draft packet id guard** validates clinic/wellness ongoing-renewal HayHashvapah draft-invoice packet path IDs before ongoing-renewal official invoice posting packet creation, rejecting malformed ongoing-renewal draft packet metadata instead of treating unsafe IDs as missing ongoing-renewal draft packets.
172. **Clinic ongoing renewal payment collection posting packet id guard** validates clinic/wellness ongoing-renewal official-invoice posting packet path IDs before ongoing-renewal payment-collection packet creation, rejecting malformed ongoing-renewal posting packet metadata instead of treating unsafe IDs as missing ongoing-renewal posting packets.
173. **Clinic ongoing renewal closeout payment collection id guard** validates clinic/wellness ongoing-renewal payment-collection packet path IDs before ongoing-renewal closeout packet creation, rejecting malformed ongoing-renewal payment-collection metadata instead of treating unsafe IDs as missing ongoing-renewal payment-collection packets.
174. **Clinic next ongoing renewal quote ongoing closeout id guard** validates clinic/wellness ongoing-renewal closeout packet path IDs before next-ongoing-renewal quote handoff creation, rejecting malformed ongoing-renewal closeout metadata instead of treating unsafe IDs as missing ongoing-renewal closeout packets.
175. **Clinic next ongoing renewal release handoff id guard** validates clinic/wellness next-ongoing-renewal quote handoff path IDs before next-ongoing-renewal quote release packet creation, rejecting malformed next-ongoing-renewal handoff metadata instead of treating unsafe IDs as missing next-ongoing-renewal quote handoffs.
176. **Clinic next ongoing renewal acceptance release id guard** validates clinic/wellness next-ongoing-renewal quote release packet path IDs before next-ongoing-renewal quote acceptance handoff creation, rejecting malformed next-ongoing-renewal release metadata instead of treating unsafe IDs as missing next-ongoing-renewal quote release packets.
177. **Clinic next ongoing renewal draft acceptance handoff id guard** validates clinic/wellness next-ongoing-renewal quote acceptance handoff path IDs before next-ongoing-renewal HayHashvapah draft-invoice packet creation, rejecting malformed next-ongoing-renewal acceptance metadata instead of treating unsafe IDs as missing next-ongoing-renewal acceptance handoffs.
178. **Clinic next ongoing renewal official invoice draft packet id guard** validates clinic/wellness next-ongoing-renewal HayHashvapah draft-invoice packet path IDs before next-ongoing-renewal official invoice posting packet creation, rejecting malformed next-ongoing-renewal draft packet metadata instead of treating unsafe IDs as missing next-ongoing-renewal draft packets.
179. **Clinic next ongoing renewal payment collection posting packet id guard** validates clinic/wellness next-ongoing-renewal official-invoice posting packet path IDs before next-ongoing-renewal payment-collection packet creation, rejecting malformed next-ongoing-renewal posting packet metadata instead of treating unsafe IDs as missing next-ongoing-renewal posting packets.
180. **Clinic next ongoing renewal closeout payment collection id guard** validates clinic/wellness next-ongoing-renewal payment-collection packet path IDs before next-ongoing-renewal closeout packet creation, rejecting malformed next-ongoing-renewal payment-collection metadata instead of treating unsafe IDs as missing next-ongoing-renewal payment-collection packets.
181. **Clinic following ongoing renewal quote next ongoing closeout id guard** validates clinic/wellness next-ongoing-renewal closeout packet path IDs before following-ongoing-renewal quote handoff creation, rejecting malformed next-ongoing-renewal closeout metadata instead of treating unsafe IDs as missing next-ongoing-renewal closeout packets.
182. **Clinic following ongoing renewal release handoff id guard** validates clinic/wellness following-ongoing-renewal quote handoff path IDs before following-ongoing-renewal quote release packet creation, rejecting malformed following-ongoing-renewal handoff metadata instead of treating unsafe IDs as missing following-ongoing-renewal quote handoffs.
183. **Clinic following ongoing renewal acceptance release id guard** validates clinic/wellness following-ongoing-renewal quote release packet path IDs before following-ongoing-renewal quote acceptance handoff creation, rejecting malformed following-ongoing-renewal release metadata instead of treating unsafe IDs as missing following-ongoing-renewal quote release packets.
184. **Clinic following ongoing renewal draft acceptance handoff id guard** validates clinic/wellness following-ongoing-renewal quote acceptance handoff path IDs before following-ongoing-renewal HayHashvapah draft-invoice packet creation, rejecting malformed following-ongoing-renewal acceptance metadata instead of treating unsafe IDs as missing following-ongoing-renewal acceptance handoffs.
185. **Clinic following ongoing renewal official invoice draft packet id guard** validates clinic/wellness following-ongoing-renewal HayHashvapah draft-invoice packet path IDs before following-ongoing-renewal official invoice posting packet creation, rejecting malformed following-ongoing-renewal draft packet metadata instead of treating unsafe IDs as missing following-ongoing-renewal draft packets.
186. **Clinic following ongoing renewal payment collection posting packet id guard** validates clinic/wellness following-ongoing-renewal official-invoice posting packet path IDs before following-ongoing-renewal payment-collection packet creation, rejecting malformed following-ongoing-renewal posting packet metadata instead of treating unsafe IDs as missing following-ongoing-renewal posting packets.
187. **Clinic following ongoing renewal closeout payment collection id guard** validates clinic/wellness following-ongoing-renewal payment-collection packet path IDs before following-ongoing-renewal closeout packet creation, rejecting malformed following-ongoing-renewal payment-collection metadata instead of treating unsafe IDs as missing following-ongoing-renewal payment-collection packets.
188. **Clinic subsequent ongoing renewal quote following ongoing closeout id guard** validates clinic/wellness following-ongoing-renewal closeout packet path IDs before subsequent-ongoing-renewal quote handoff creation, rejecting malformed following-ongoing-renewal closeout metadata instead of treating unsafe IDs as missing following-ongoing-renewal closeout packets.
189. **Clinic subsequent ongoing renewal release handoff id guard** validates clinic/wellness subsequent-ongoing-renewal quote handoff path IDs before subsequent-ongoing-renewal quote release packet creation, rejecting malformed subsequent-ongoing-renewal handoff metadata instead of treating unsafe IDs as missing subsequent-ongoing-renewal quote handoffs.
190. **Clinic subsequent ongoing renewal acceptance release id guard** validates clinic/wellness subsequent-ongoing-renewal quote release packet path IDs before subsequent-ongoing-renewal quote acceptance handoff creation, rejecting malformed subsequent-ongoing-renewal release metadata instead of treating unsafe IDs as missing subsequent-ongoing-renewal quote release packets.
191. **Clinic subsequent ongoing renewal draft acceptance handoff id guard** validates clinic/wellness subsequent-ongoing-renewal acceptance handoff path IDs before subsequent-ongoing-renewal HayHashvapah draft-invoice packet creation, rejecting malformed subsequent-ongoing-renewal acceptance metadata instead of treating unsafe IDs as missing subsequent-ongoing-renewal acceptance handoffs.
192. **Clinic subsequent ongoing renewal official invoice draft packet id guard** validates clinic/wellness subsequent-ongoing-renewal HayHashvapah draft-invoice packet path IDs before subsequent-ongoing-renewal official-invoice posting packet creation, rejecting malformed subsequent-ongoing-renewal draft metadata instead of treating unsafe IDs as missing subsequent-ongoing-renewal draft packets.
193. **CRM lead conversion path id guard** validates CRM lead conversion path IDs before lead lookup or conversion work, rejecting malformed lead metadata instead of treating unsafe IDs as missing leads.
194. **CRM deal forecast path id guard** validates CRM deal forecast path IDs before forecast lookup or update work, rejecting malformed deal metadata instead of treating unsafe IDs as missing deals.
195. **CRM quote approval path id guard** validates CRM app entitlement plus CRM quote release approval path IDs before quote lookup or workflow approval work, rejecting unauthorized or malformed quote metadata instead of treating unsafe IDs as missing quotes.
196. **CRM collection path id guard** validates CRM collection task and promise reminder path IDs before payment-promise or reminder lookup work, rejecting malformed collection route metadata instead of treating unsafe IDs as missing collection records.
197. **Docs document path id guard** validates Docs document path IDs before document reads, edits, signer, send, sign, void, or export work, rejecting malformed document route metadata instead of treating unsafe IDs as missing documents.
198. **Projects path id guard** validates Projects project, task, and milestone path IDs before reads, edits, child writes, time logging, billing preview, or bill-time work, rejecting malformed project route metadata instead of treating unsafe IDs as missing project records.
199. **Service Desk case path id guard** validates Service Desk case path IDs before replies, escalation, resolution, or case update work, rejecting malformed service route metadata before service case lookup or support evidence mutation.
200. **People-HR employee path id guard** validates People-HR employee path IDs before employee updates, payroll runs, or per-employee payroll history reads, rejecting malformed people route metadata before employee lookup or payroll evidence mutation.
201. **Workflow approval/run path id guard** validates workflow approval and run path IDs before approval decisions, approval execution, or failed-run retry work, rejecting malformed workflow route metadata before workflow lookup or action side effects.
202. **Finance mutation path id guard** validates finance draft invoice, invoice, and bank transaction path IDs before draft posting, payment receipt, or bank reconciliation work, rejecting malformed finance route metadata before finance lookup or accounting side effects.
203. **Legal source path id guard** validates Armenian legal-source review path IDs before reviewer-role checks or source review work, rejecting malformed legal-source route metadata before legal source lookup, review, block-event, or audit side effects.
204. **Finance period path key guard** validates finance period close/reopen path keys before finance period lookup or period-state mutation, rejecting malformed finance period route metadata before period lock, reopen, suite-event, audit, or downstream accounting side effects.
205. **App assignment path id guard** validates suite app assignment path IDs before app lookup, assignment writes, access-review impact, or audit evidence, rejecting malformed product-access route metadata before dashboard entitlement side effects.
206. **Finance bill payment path id guard** validates supplier bill payment path IDs before bill lookup or payment work, rejecting malformed bill route metadata before AP payment, ledger, status, or audit side effects.
207. **Admin evidence path id guard** validates admin session, audit-export, and tenant-backup path IDs before session lookup, audit-export payload read, or restore-proof work, rejecting malformed admin evidence route metadata before revocation, payload disclosure, restore-proof audit, or backup side effects.
208. **Customer 360 path id guard** validates customer path IDs before the cross-module Customer 360 lookup, rejecting malformed customer route metadata before CRM, finance, service, workflow, legal, privacy, or advisory graph reads.
209. **Workflow rule path id guard** validates workflow rule path IDs before versions, state, rollback, dry-run, or test-event work, rejecting malformed rule route metadata before version, event, dry-run, test-event, or audit side effects.
210. **Integration connector route key guard** normalizes connector route keys before configure, health-check, or connector response reload work, rejecting malformed connector route metadata before config writes, health evidence, suite events, or audit side effects.
211. **Webhook delivery route id guard** normalizes webhook delivery retry path IDs before delivery lookup or retry dispatch, rejecting malformed retry route metadata before outbound retry attempts, delivery status changes, or audit-side evidence.
212. **Forms definition route id guard** normalizes Forms definition detail and patch path IDs before definition lookup or update work, rejecting malformed form route metadata before submission reads, definition updates, lead evidence, or audit-side evidence.
213. **Service case route id guard** normalizes Service Desk mutation path IDs before case lookup, reply, escalation, resolution, or patch work, rejecting malformed service route metadata before support messages, escalation/resolution rows, suite events, or audit-side evidence.
214. **Workflow finance route id guard** normalizes workflow-run retry, draft-invoice post, bank-transaction reconcile, and invoice-payment path IDs at the route boundary before lookup or mutation work, rejecting malformed workflow/finance route metadata before retry, posting, reconciliation, payment, ledger, suite-event, or audit evidence.
215. **Workflow approval route id guard** normalizes workflow approval decision and execution path IDs at the route boundary before decision-body parsing, approval lookup, execution, suite-event, or audit work, rejecting malformed approval route metadata before body secrets can influence the error path.
216. **Legal ingest article marker guard** detects Armenian article headings through an offset-preserving homoglyph shadow while preserving the original stored legal text, and keeps genuine Latin `Article N` markers on the original text path so sovereign legal KB ingestion stays article-aware for OCR-corrupted and Latin sources.
217. **Native legal PDF ingest** reads `.pdf` law sources through the local `pdftotext` binary when available, skips PDFs cleanly when the binary is absent, and keeps `.txt`/`.md` sovereign legal KB ingestion dependency-lean and offline by default.
218. **PDF availability probe hardening** treats any `pdftotext -v` spawn error or non-zero probe exit as unavailable, so legal PDF ingestion stays on the graceful skip path when the binary is missing, blocked, or not runnable instead of attempting extraction later.
219. **PDF extractor unavailable skip hardening** canonicalizes extractor-level `PdftotextUnavailableError` failures to the same `pdftotext-unavailable` skip reason as the preflight probe, keeping CLI install guidance and no-crash PDF skip behavior stable if the binary disappears or becomes unavailable between probe and extraction.
220. **PDF extraction spawn unavailable hardening** maps blocked or missing `pdftotext` extraction spawn errors (`ENOENT`, `EACCES`, `EPERM`) to `PdftotextUnavailableError`, keeping late binary-permission changes on the same typed legal-PDF skip path while preserving non-zero parse failures as real extraction errors.
221. **Legal source regular-file guard** skips extension-named directories or other non-file entries before `.txt`/`.md` reads or `.pdf` extraction, preventing malformed law-source folders from crashing offline KB ingestion or invoking `pdftotext` on directories.
222. **Legal source symlink guard** skips extension-named symlinks before `.txt`/`.md` reads or `.pdf` extraction, preventing linked law-source entries from escaping the selected source directory or invoking PDF extraction on symlink targets.
223. **Legal source hidden-file guard** skips dot-prefixed extension-named law-source sidecars before `.txt`/`.md` reads or `.pdf` extraction, preventing AppleDouble/hidden draft files from becoming legal KB sources or PDF extraction inputs.
224. **Legal source text-read guard** skips unreadable `.txt`/`.md` law-source files with a `read-failed` reason instead of aborting the whole offline legal KB build, while preserving readable source ingestion.

Sovereign foundation: outbound network **off by default** + opt-in egress allowlist (loopback always allowed); data dir outside the repo (OS app-support); optional bundled local AI (Ollama); offline Armenian legal RAG (BM25 + optional hybrid). One-command install (`deploy/install.sh`, launchd/systemd templates, WAL backup).

---

## 2. Architecture

- **`server/index.js`** — boot (host/port/dbPath from `server/config.js`).
- **`server/config.js`** — local-server policy: data-dir relocation, egress gate (`assertEgressAllowed`, deny-until-listed, loopback-always, 403), local-AI defaults.
- **`server/db.js`** (~12k lines) — `node:sqlite` `DatabaseSync`; full schema in `initSchema`; `seedIfEmpty` demo org/data; `ensure*Layer(db)` idempotent migrations (PRAGMA table_info + ALTER); effective-dated rate resolvers (`resolvePayrollConfig`/`resolveVatRate`).
- **`server/app.js`** (~49k lines) — Fastify; `buildApp({dbPath})`; cookie auth via `app.auth`; all routes in `registerApi`; role gates (`requireOwner`/`requireFinanceOperator`/`requireDocsWriter`/`requireProjectsWriter`/`requirePeopleWriter`/…); per-IP `enforceRateLimit`. No global auth hook — auth is opt-in per route (so public routes just omit `app.auth`).
- **`server/ledger.js`** — double-entry posting (period-lock-aware, idempotent via unique source index); tenant ledger accounts seed/refresh from the generated 623-account official RA chart, with new input VAT posted to `226` and legacy local `526` rows read only for backwards-compatible VAT reports.
- **`server/accounting.js`** / **`server/payroll.js`** / **`server/rag.js`** — verbatim ports (engine-underneath).
- **`web/src/main.jsx`** (~12k lines) — single SPA. **Data-presence render model (no router)**: panels render when their data is present. One bundled state object per app, fetched in `load()`'s app-gate. `api(path,{method,body})` cookie-auth helper that throws `new Error(data.error)`. Per-app panels in focused modules: `crm.jsx`, `finance.jsx`, `desk.jsx`, `people.jsx`, `docs.jsx`, `projects.jsx`, `forms.jsx`.

**Add-a-view pattern** (consistent across the whole app): new panel file → import into main.jsx → +1 fetch in the load() app-gate → +1 key on the bundled state object → render-by-presence. No new top-level state, no `Workspace` signature growth (the ~86 pilot props were bundled into one `pilot` object).

---

## 3. Running & verifying

```bash
# Boot (data dir defaults to OS app-support; override for a throwaway run)
PORT=4178 ARMOSPHERA_ONE_DB=/tmp/aoc.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 node server/index.js
# Login: owner@armosphera.local / change-me-now   (also: accountant@, lawyer@, operator@, support@, auditor@, …)
npm run build:ui     # vite build → public/
npm test             # node --test --test-concurrency=4 --test-timeout=60000
```

### OPPO remote-control / live preview

Run from `~/dev/A1-Suite-Local`:

```bash
PORT=4178 HOST=0.0.0.0 ARMOSPHERA_ONE_DB=/tmp/a1-suite-event-form-ui.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 node server/index.js
```

Open from OPPO on the same LAN using the exact URL printed by:

```bash
MAC_IP=$(ipconfig getifaddr en0 || ipconfig getifaddr en1)
printf 'http://%s:4178/\n' "$MAC_IP"
```

The Copilot slice is Armenian-first and now uses OpenRouter as the single opt-in cloud provider (`COPILOT_PROVIDER=openrouter`, `COPILOT_LANGUAGE=hy-AM`) with a live model menu plus local-first fallback. Open Notebook is an opt-in supplemental-source connector only; it sits beside the in-process sovereign RAG layer and cannot satisfy legal citation or professional-review gates. Local verification keeps execution deterministic with outbound disabled by default.

Current checkpoint:
- Current Legal source text-read guard checkpoint: `f893b7f` (`Harden law source text read failures`) on `codex/law-source-text-read-guard` (skips unreadable `.txt`/`.md` law-source files with `read-failed`, keeping one bad text source from aborting the whole offline legal KB build).
- Latest Legal source text-read guard verification from `~/dev/A1-Suite-Local`: `node --check scripts/ingest-laws.js && node --check test/pdf-text.test.js && node --check server/pdfText.js` pass; focused PDF/source-reader suite pass (`node --test test/pdf-text.test.js`, 13 pass, 0 fail, 0 cancelled); broader legal/RAG ingest suite pass (`node --test test/pdf-text.test.js test/law-ingest.test.js test/law-ingest-homoglyph.test.js test/law-ingest-build.test.js test/law-embed.test.js test/rag.test.js test/legal-search.test.js test/legal-grounding.test.js`, 41 pass, 0 fail, 0 cancelled); local diff review found no issues; `git diff --check` pass; capped full `npm test` pass (553 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-law-source-text-read-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Current Legal source hidden-file guard checkpoint: `a4d6718` (`Harden law source hidden file filtering`) on `codex/law-source-hidden-file-guard` (skips dot-prefixed extension-named sidecars before text reads or PDF extraction, keeping AppleDouble and hidden draft files out of offline law-source ingestion).
- Latest Legal source hidden-file guard verification from `~/dev/A1-Suite-Local`: `node --check scripts/ingest-laws.js && node --check test/pdf-text.test.js && node --check server/pdfText.js` pass; focused PDF/source-reader suite pass (`node --test test/pdf-text.test.js`, 12 pass, 0 fail, 0 cancelled); broader legal/RAG ingest suite pass (`node --test test/pdf-text.test.js test/law-ingest.test.js test/law-ingest-homoglyph.test.js test/law-ingest-build.test.js test/law-embed.test.js test/rag.test.js test/legal-search.test.js test/legal-grounding.test.js`, 40 pass, 0 fail, 0 cancelled); local diff review found no issues; `git diff --check` pass; capped full `npm test` pass (552 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-law-source-hidden-file-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Current test runner safety checkpoint: `ee82f1e` (`chore(test): cap node --test concurrency to prevent OOM disk-fill`) on `main` and `codex/suite-dashboard-route-normalization` (runs `npm test` as `node --test --test-concurrency=4 --test-timeout=60000`, preventing the previous unbounded full-suite worker fan-out from exhausting disk/memory on this Mac).
- Current sidebar test cleanup checkpoint: `724bc23` (`Harden sidebar test cleanup`) on `codex/law-source-symlink-guard` (ensures the browser-backed sidebar test closes the Fastify app even if Chromium launch fails and closes per-role browser contexts on assertion failures).
- Current Legal source symlink guard checkpoint: `b53c091` (`Harden law source symlink filtering`) on `codex/law-source-symlink-guard` (skips extension-named symlinks before text reads or PDF extraction, keeping offline law-source ingestion scoped to regular source files).
- Latest Legal source symlink guard verification from `~/dev/A1-Suite-Local`: `node --check scripts/ingest-laws.js && node --check test/pdf-text.test.js && node --check server/pdfText.js` pass; focused PDF/source-reader suite pass (`node --test test/pdf-text.test.js`, 11 pass, 0 fail, 0 cancelled); broader legal/RAG ingest suite pass (`node --test test/pdf-text.test.js test/law-ingest.test.js test/law-ingest-homoglyph.test.js test/law-ingest-build.test.js test/law-embed.test.js test/rag.test.js test/legal-search.test.js test/legal-grounding.test.js`, 39 pass, 0 fail, 0 cancelled); read-only subagent review unavailable because the agent thread limit was reached; local diff review found no issues; `git diff --check` pass; capped full `npm test` pass (551 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-law-source-symlink-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Current Legal source regular-file guard checkpoint: `ddc91b2` (`Harden law source regular file filtering`) (skips extension-named directories/non-files before text reads or PDF extraction, preserving offline law-source ingestion when source folders contain archive directories or other non-regular entries).
- Latest Legal source regular-file guard verification from `~/dev/A1-Suite-Local`: `node --check scripts/ingest-laws.js && node --check test/pdf-text.test.js && node --check server/pdfText.js` pass; focused PDF/source-reader suite pass (`node --test test/pdf-text.test.js`, 10 pass, 0 fail, 0 cancelled); broader legal/RAG ingest suite pass (`node --test test/pdf-text.test.js test/law-ingest.test.js test/law-ingest-homoglyph.test.js test/law-ingest-build.test.js test/law-embed.test.js test/rag.test.js test/legal-search.test.js test/legal-grounding.test.js`, 38 pass, 0 fail, 0 cancelled); `git diff --check` pass; capped full `npm test` pass (550 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-law-source-regular-file-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Previous PDF extraction spawn unavailable hardening checkpoint: `ecaa6e2` (`Harden PDF extraction spawn unavailable errors`) (maps missing or blocked `pdftotext` extraction spawn errors to typed `PdftotextUnavailableError`, preserving canonical legal-PDF skip behavior if binary availability changes after the probe).
- Latest PDF extraction spawn unavailable hardening verification from `~/dev/A1-Suite-Local`: `node --check server/pdfText.js && node --check test/pdf-text.test.js && node --check scripts/ingest-laws.js` pass; focused PDF text suite pass (`node --test test/pdf-text.test.js`, 9 pass, 0 fail, 0 cancelled); broader legal/RAG ingest suite pass (`node --test test/pdf-text.test.js test/law-ingest.test.js test/law-ingest-homoglyph.test.js test/law-ingest-build.test.js test/law-embed.test.js test/rag.test.js test/legal-search.test.js test/legal-grounding.test.js`, 37 pass, 0 fail, 0 cancelled); `git diff --check` pass; capped full `npm test` pass (549 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-pdf-extraction-spawn-unavailable-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Previous PDF extractor unavailable skip hardening checkpoint: `541fbc7` (`Harden PDF extractor unavailable skips`) (canonicalizes extractor-thrown `PdftotextUnavailableError` to the `pdftotext-unavailable` skip reason, preserving CLI install guidance and skip behavior when availability changes after the probe).
- Latest PDF extractor unavailable skip hardening verification from `~/dev/A1-Suite-Local`: `node --check scripts/ingest-laws.js && node --check test/pdf-text.test.js && node --check server/pdfText.js` pass; focused PDF text suite pass (`node --test test/pdf-text.test.js`, 8 pass, 0 fail, 0 cancelled); broader legal/RAG ingest suite pass (`node --test test/pdf-text.test.js test/law-ingest.test.js test/law-ingest-homoglyph.test.js test/law-ingest-build.test.js test/law-embed.test.js test/rag.test.js test/legal-search.test.js test/legal-grounding.test.js`, 36 pass, 0 fail, 0 cancelled); `git diff --check` pass; capped full `npm test` pass (548 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-pdf-extractor-unavailable-skip-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Previous PDF availability probe hardening checkpoint: `16d6ea7` (`Harden PDF availability probe`) on `main` (makes the `pdftotext` availability probe fail closed on spawn errors and non-zero version probes, preserving graceful PDF skip behavior for blocked or unrunnable binaries).
- Latest PDF availability probe hardening verification from `~/dev/A1-Suite-Local`: `node --check server/pdfText.js && node --check test/pdf-text.test.js && node --check scripts/ingest-laws.js` pass; focused PDF text suite pass (`node --test test/pdf-text.test.js`, 7 pass, 0 fail, 0 cancelled); `git diff --check` pass; capped full `npm test` pass (547 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-pdf-availability-probe-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Previous Native legal PDF ingest checkpoint: `05a0d6e` (`feat(rag): native PDF ingest via pdftotext shell-out`) on `codex/suite-dashboard-route-normalization` (adds dependency-injected `pdftotext` extraction for `.pdf` legal sources, keeps missing-poppler behavior graceful by skipping PDFs with a warning, and preserves existing `.txt`/`.md` offline KB ingestion).
- Latest Native legal PDF ingest verification from `~/dev/A1-Suite-Local`: `node --check server/pdfText.js && node --check scripts/ingest-laws.js && node --check test/pdf-text.test.js && node --check server/lawIngest.js && node --check test/law-ingest.test.js && node --check test/law-ingest-homoglyph.test.js` pass; focused PDF/legal/RAG ingest suite pass (`node --test test/pdf-text.test.js test/law-ingest.test.js test/law-ingest-homoglyph.test.js test/law-ingest-build.test.js test/law-embed.test.js test/rag.test.js test/legal-search.test.js test/legal-grounding.test.js`, 35 pass, 0 fail, 0 cancelled); `git diff --check` pass; capped full `npm test` pass (547 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-native-pdf-ingest-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Previous Legal ingest article marker guard checkpoint: `435844e` (`Harden legal ingest article markers`) on `codex/suite-dashboard-route-normalization` (detects OCR-corrupted Armenian article headings through an offset-preserving homoglyph shadow, preserves original legal text bytes, and keeps genuine Latin `Article N` source splitting intact).
- Latest Legal ingest article marker guard verification from `~/dev/A1-Suite-Local`: `node --check server/lawIngest.js && node --check test/law-ingest.test.js && node --check test/law-ingest-homoglyph.test.js` pass; focused legal/RAG ingest suite pass (`node --test test/law-ingest.test.js test/law-ingest-homoglyph.test.js test/law-ingest-build.test.js test/law-embed.test.js test/rag.test.js test/legal-search.test.js test/legal-grounding.test.js`, 28 pass, 0 fail, 0 cancelled); read-only subagent review unavailable because the agent thread limit was reached; local diff review found no issues; `git diff --check` pass; full verification is covered by the current PDF availability probe hardening checkpoint.
- Previous Workflow approval route id guard checkpoint: `7ba8ca0` (`Harden workflow approval route ids`) on `codex/suite-dashboard-route-normalization` (normalizes workflow approval decision and execution path IDs at the route boundary before decision-body parsing, approval lookup, execution, suite-event, or audit work).
- Latest Workflow approval route id guard verification from `~/dev/A1-Suite-Local`: `node --check server/app.js && node --check test/api.test.js` pass; focused workflow approval route regression pass (`node --test --test-name-pattern "workflow approval decision rejects malformed metadata" test/api.test.js`, 1 pass, 0 fail, 0 cancelled); broader workflow/API regression pass (`node --test --test-name-pattern "workflow approval decision rejects malformed metadata|owner approval decision updates workflow queue|approved service reply workflow sends|approved overdue-invoice workflow executes|closed finance period records failed workflow run and retries|workflow dry-run|workflow test-event|workflow list query filters|workflow rule state and rollback" test/api.test.js`, 11 pass, 0 fail, 0 cancelled); read-only subagent review unavailable because the agent thread limit was reached; local diff review found no issues; `git diff --check` pass; full verification is covered by the current PDF availability probe hardening checkpoint.
- Current Workflow finance route id guard checkpoint: `30c5e13` (`Harden workflow finance route ids`) on `codex/suite-dashboard-route-normalization` (normalizes workflow-run retry, draft-invoice post, bank-transaction reconcile, and invoice-payment path IDs at the route boundary before lookup or mutation work, keeping malformed workflow/finance route IDs from leaking submitted secrets or changing retry/accounting evidence).
- Latest Workflow finance route id guard verification from `~/dev/A1-Suite-Local`: `node --check server/app.js && node --check test/api.test.js` pass; focused workflow/finance path regression pass (`node --test --test-name-pattern "workflow approval decision rejects malformed metadata|closed finance period records failed workflow run and retries|draft invoice posting rejects malformed metadata|payment receipt rejects malformed metadata|bank transaction import rejects malformed metadata" test/api.test.js`, 5 pass, 0 fail, 0 cancelled); broader workflow/finance regression pass (`node --test --test-name-pattern "workflow|draft invoice posting rejects|payment receipt rejects|bank transaction import rejects|finance period guardrails|closed finance period|SRC export|Armenian bank transaction import reconciles|posted HayHashvapah draft invoice creates|payment receipt marks" test/api.test.js`, 40 pass, 0 fail, 0 cancelled); read-only subagent review unavailable because the agent thread limit was reached; local diff review found no issues; `git diff --check` pass; full `npm test` pass (536 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-workflow-finance-route-id-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Previous Sovereign legal KB build + embedding pipeline checkpoint: `6cc0d43` (`feat(rag): buildLawsDb + ingest-laws CLI — text files → installable laws.sqlite`) plus `6efab5f` docs/source cleanup on `codex/suite-dashboard-route-normalization` (builds installable `law_chunks` SQLite databases from raw Armenian `.txt`/`.md` law sources, preserving article-aware chunks for the existing offline BM25 RAG index, with opt-in local embeddings that degrade safely back to BM25).
- Latest Sovereign legal KB build + embedding pipeline verification from `~/dev/A1-Suite-Local`: `node --check server/lawIngest.js && node --check server/lawEmbedIngest.js && node --check server/rag.js && node --check scripts/ingest-laws.js && node --check test/law-ingest.test.js && node --check test/law-ingest-build.test.js && node --check test/law-embed.test.js && node --check test/rag.test.js` pass; focused legal/RAG ingest + embedding suite pass (`node --test test/law-ingest.test.js test/law-ingest-build.test.js test/law-embed.test.js test/rag.test.js test/legal-search.test.js test/legal-grounding.test.js`, 24 pass, 0 fail, 0 cancelled); read-only subagent review unavailable because the agent thread limit was reached; local diff review found the imported source file had literal NUL separators, now normalized to escaped `\\0` separators; full `npm test` pass (536 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-law-ingest-build-regression-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Previous Service case route id guard checkpoint: `52c7f3b` (`Harden service case route ids`) on `codex/suite-dashboard-route-normalization` (normalizes Service Desk mutation path IDs before case lookup, reply, escalation, resolution, or patch work, keeping malformed route IDs from leaking route/body secrets or changing service evidence).
- Latest Service case route id guard verification from `~/dev/A1-Suite-Local`: `node --check server/app.js && node --check test/api.test.js && node --check test/service-cases.test.js` pass; focused service route regression pass (`node --test --test-name-pattern "service case mutations reject malformed metadata|service console exposes customers|PATCH cannot de-escalate|service-cases isolation" test/api.test.js test/service-cases.test.js`, 3 pass, 0 fail, 0 cancelled); broader service/API/People/payroll suite pass (`node --test test/api.test.js test/service-cases.test.js test/people-hr.test.js test/payroll-endpoints.test.js test/payroll-employee-fk.test.js test/payroll.test.js test/tax-rate-versioning.test.js`, 248 pass, 0 fail, 0 cancelled); read-only subagent review unavailable because the agent thread limit was reached; local line-numbered diff review found no issues; `git diff --check` pass; full `npm test` pass (526 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-service-route-id-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Previous Forms definition route id guard checkpoint: `7e452e4` (`Harden form definition route ids`) on `codex/suite-dashboard-route-normalization` (normalizes Forms detail and patch path IDs before form lookup, submission reads, and definition update work, keeping malformed route IDs from leaking route/body secrets or changing form definitions).
- Previous Webhook delivery route id guard checkpoint: `a7f8507` (`Harden webhook delivery route ids`) on `codex/suite-dashboard-route-normalization` (normalizes webhook delivery retry path IDs before delivery lookup and retry dispatch, keeping malformed retry paths from incrementing attempts, changing status, or leaking submitted route/body secrets).
- Previous Integration connector route key guard checkpoint: `9a177b6` (`Harden integration connector route keys`) on `codex/suite-dashboard-route-normalization` (normalizes connector route keys before configure, health-check, and health-check response reload work, keeping connector evidence and audit rows tied to canonical connector definitions).
- Previous Workflow rule path id guard checkpoint: `9c39abd` (`Harden workflow rule path ids`) on `codex/suite-dashboard-route-normalization` (blocks unsafe workflow rule path IDs before versions reads, state changes, rollbacks, dry-runs, test-events, suite events, audit records, or workflow evidence rows).
- Previous Customer 360 path id guard checkpoint: `7b21995` (`Harden customer 360 path ids`) on `codex/suite-dashboard-route-normalization` (blocks unsafe Customer 360 path IDs before customer lookup and the CRM, finance, service, workflow, legal, privacy, and advisory graph read).
- Previous Admin evidence path id guard checkpoint: `584f80e` (`Harden admin evidence path ids`) on `codex/suite-dashboard-route-normalization` (blocks unsafe admin session, audit-export, and tenant-backup path IDs before session lookup, audit-export payload read, restore-proof generation, restore-proof audit, or backup side effects).
- Current Finance bill payment path id guard checkpoint: `ab15c0d` (`Harden finance bill payment path ids`) on `codex/suite-dashboard-route-normalization` (blocks unsafe supplier bill payment path IDs before bill lookup, AP payment, ledger, status, or audit side effects).
- Latest Finance bill payment path id guard verification from `~/dev/A1-Suite-Local`: `node --check server/app.js && node --check test/payables-endpoints.test.js` pass; focused bill-payment path regression pass (`node --test --test-name-pattern "bill payment rejects malformed metadata" test/payables-endpoints.test.js`, 1 pass, 0 fail, 0 cancelled); broader payables/finance suite pass (`node --test test/payables-endpoints.test.js test/payables-idempotency.test.js test/finance-rbac.test.js test/ledger-core.test.js test/accounting-payables.test.js`, 10 pass, 0 fail, 0 cancelled); sidebar openability proof pass after restoring Playwright Chromium (`node --test test/suite-dashboard-sidebar-openability.test.mjs`, 1 pass, 0 fail, 0 cancelled); `git diff --check` pass; full `npm test` pass (508 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-finance-bill-path-id-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Current App assignment path id guard checkpoint: `16ebf44` (`Harden finance period path keys`), docs synced in `9fdc3a5` on `codex/suite-dashboard-route-normalization` (blocks unsafe app assignment path IDs before app lookup, role validation, assignment writes, access-review impact, or audit side effects).
- Latest App assignment path id guard verification from `~/dev/A1-Suite-Local`: `node --check server/app.js && node --check test/api.test.js` pass; focused app-assignment/dashboard route regression pass (`node --test --test-name-pattern "owner can update app assignment|app assignment rejects non-boolean|app assignment treats omitted enabled|app assignment rejects malformed metadata|app assignment rejects unknown roles|dashboard launcher source wiring covers every seeded login role app" test/api.test.js test/suite-routes.test.mjs test/suite-dashboard-sidebar-openability.test.mjs`, 8 pass, 0 fail, 0 cancelled); broader app-assignment/access/sidebar/finance-period suite pass (`node --test --test-name-pattern "app assignment|dashboard launcher|access review|suite route|sidebar opens|auditor-readonly|canonical app ids|role-based app entitlements|finance period guardrails|finance period close rejects|closed finance period blocks SRC export|closed finance period records failed workflow run and retries|posted HayHashvapah draft invoice creates|draft invoice posting rejects|closed finance period blocks posting|payment receipt marks|payment receipt rejects|closed finance period blocks payment|finance report query filters reject|SRC export rejects|bank transaction import rejects|Armenian bank transaction import reconciles" test/api.test.js test/auditor-readonly-coverage.test.js test/suite-routes.test.mjs test/suite-dashboard-sidebar-openability.test.mjs`, 31 pass, 0 fail, 0 cancelled); read-only subagent review unavailable because the agent thread limit was reached; local line-numbered diff review found no issues; `git diff --check` pass; full `npm test` pass (508 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-app-assignment-finance-period-path-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Current Finance period path key guard checkpoint: `16ebf44` (`Harden finance period path keys`), docs synced in `5ca973f` on `codex/suite-dashboard-route-normalization` (blocks unsafe finance period close/reopen path keys before finance period lookup, period lock, reopen, suite-event, audit, or downstream accounting side effects).
- Latest Finance period path key guard verification from `~/dev/A1-Suite-Local`: `node --check server/app.js && node --check test/api.test.js` pass; `git diff --check` pass; focused finance period path-key regression pass (`node --test --test-name-pattern "finance period close rejects malformed metadata" test/api.test.js`, 1 pass, 0 fail, 0 cancelled); broader finance/accounting action suite pass (`node --test --test-name-pattern "finance period guardrails|finance period close rejects|closed finance period blocks SRC export|closed finance period records failed workflow run and retries|posted HayHashvapah draft invoice creates|draft invoice posting rejects|closed finance period blocks posting|payment receipt marks|payment receipt rejects|closed finance period blocks payment|finance report query filters reject|SRC export rejects|bank transaction import rejects|Armenian bank transaction import reconciles" test/api.test.js`, 14 pass, 0 fail, 0 cancelled); read-only subagent review pass (code/security/docs, no findings); full `npm test` pass (508 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-finance-period-path-key-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Current Legal source path id guard checkpoint: `1813c4a` (`Harden legal source review path ids`), pushed on `codex/suite-dashboard-route-normalization` (blocks unsafe legal-source review path IDs before reviewer-role checks, legal source lookup, source review, block-event, suite-event, or audit side effects).
- Latest Legal source path id guard verification from `~/dev/A1-Suite-Local`: `node --check server/app.js && node --check test/api.test.js` pass; focused legal source regression pass (`node --test --test-name-pattern "legal source review rejects malformed metadata|owner can review and version Armenian legal source registry entries|legal source review keeps reviewed URLs|legal source review does not downgrade|legal source review rejects credentialed|non-owner cannot review legal source" test/api.test.js`, 6 pass, 0 fail, 0 cancelled); broader legal/API regression pass (`node --test --test-name-pattern "legal source|production-readiness|SRC export requires reviewed VAT legal source|customer 360 joins CRM, finance, service, automation, and legal sources|legal answer|personal-data consent" test/api.test.js`, 11 pass, 0 fail, 0 cancelled); read-only subagent review unavailable because the agent thread limit was reached; local line-numbered diff review found no issues; `git diff --check` pass; full `npm test` pass (508 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-legal-source-path-id-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Current Workflow and Finance path id guard checkpoint: `851108e` (`Harden workflow approval path ids`), docs synced in `6f5718c` on `codex/suite-dashboard-route-normalization` (blocks unsafe workflow approval/run path IDs before approval decisions, approval execution, retry, suite-event, audit, action, or workflow-run side effects; blocks unsafe finance draft invoice, invoice, and bank transaction path IDs before draft posting, payment receipt, reconciliation, ledger, suite-event, audit, webhook, or collection side effects).
- Latest Workflow and Finance path id guard verification from `~/dev/A1-Suite-Local`: `node --check server/app.js && node --check test/api.test.js` pass; focused Workflow/Finance path regression pass (`node --test --test-name-pattern "workflow approval decision rejects malformed metadata|draft invoice posting rejects malformed metadata|payment receipt rejects malformed metadata|bank transaction import rejects malformed metadata" test/api.test.js`, 4 pass, 0 fail, 0 cancelled); broader API/finance/accounting suite pass (`node --test test/api.test.js test/finance-posting.test.js test/ledger-core.test.js test/opening-balances.test.js test/payroll.test.js test/payroll-endpoints.test.js test/tax-rate-versioning.test.js`, 246 pass, 0 fail, 0 cancelled); `git diff --check` pass; full `npm test` pass (508 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-workflow-finance-path-id-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Current Service Desk and People-HR path id guard checkpoint: `b90c8ca` (`Harden service and people path ids`), pushed on `codex/suite-dashboard-route-normalization` (blocks unsafe Service Desk case path IDs before reply, escalation, resolution, update, suite-event, audit, message, escalation, or resolution side effects; blocks unsafe People-HR employee path IDs before employee updates, employee payroll runs, payroll history reads, payroll run, ledger, or audit side effects).
- Latest Service Desk and People-HR path id guard verification from `~/dev/A1-Suite-Local`: `node --check server/app.js && node --check test/api.test.js && node --check test/people-hr.test.js` pass; focused Service Desk/People-HR path regression pass (`node --test --test-name-pattern "service case mutations reject malformed metadata|people-hr: malformed employee path ids" test/api.test.js test/people-hr.test.js`, 2 pass, 0 fail, 0 cancelled); broader Service Desk/API/People-HR/payroll suite pass (`node --test test/api.test.js test/people-hr.test.js test/payroll-endpoints.test.js test/payroll-employee-fk.test.js test/payroll.test.js test/tax-rate-versioning.test.js`, 246 pass, 0 fail, 0 cancelled); read-only subagent review pass (code/security/docs, no findings); `git diff --check` pass; full `npm test` pass (508 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-service-people-path-id-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Current Projects path id guard checkpoint: `f234c99` (`Test Projects path router rejections`), pushed on `codex/suite-dashboard-route-normalization` (blocks unsafe Projects project/task/milestone path IDs before project lookup, child mutation, time-entry, billing-preview, bill-time, finance, suite-event, or audit work; finalizes the router-level 400/404 regression contract on top of `390383a`).
- Latest Projects path id guard verification from `~/dev/A1-Suite-Local`: `node --check server/app.js && node --check test/projects.test.js && node --check test/project-billing.test.js` pass; `git diff --check` pass; focused Projects path/billing path regression pass (`node --test --test-name-pattern "projects: malformed path ids|project-billing: malformed project path ids" test/projects.test.js test/project-billing.test.js`, 2 pass, 0 fail, 0 cancelled); broader Projects/billing suite pass (`node --test test/projects.test.js test/project-billing.test.js`, 11 pass, 0 fail, 0 cancelled); read-only subagent review pass (code/security, no findings; residual overlong route status delegated to router rejection but no side-effect risk); full `npm test` pass (507 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-projects-path-id-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Current Docs document path id guard checkpoint: `980d917` (`Harden Docs document path ids`), pushed on `codex/suite-dashboard-route-normalization` (blocks unsafe Docs document path IDs before document lookup, edit, signer, send, sign, void, export, suite-event, or audit work).
- Latest Docs document path id guard verification from `~/dev/A1-Suite-Local`: `node --check server/app.js && node --check test/docs-sign.test.js && node --check test/docs-export.test.js` pass; `git diff --check` pass; focused Docs document path/malformed metadata regression pass (`docs-sign: malformed document metadata` and `docs-export: unknown id`, 2 pass); broader Docs sign/export/template suite pass (`node --test test/docs-sign.test.js test/docs-export.test.js test/docs-templates.test.js`, 19 pass, 0 fail, 0 cancelled); read-only subagent review pass (code/security, no findings); full `npm test` pass (505 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-docs-document-path-id-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Current CRM collection path id guard checkpoint: `2da5c3d` (`Harden CRM collection path ids`), pushed on `codex/suite-dashboard-route-normalization` (blocks unsafe CRM collection task and promise path IDs before promise creation, reminder delivery, task-state updates, suite-event, or audit work).
- Latest CRM collection path id guard verification from `~/dev/A1-Suite-Local`: `node --check server/app.js && node --check test/api.test.js` pass; focused CRM collection path/malformed metadata regression pass (`collection promise and reminder reject malformed metadata before persistence`, 1 pass, including unsafe/overlong/encoded task and promise path IDs); broader collection/CRM guard suite pass (`node --test --test-name-pattern "collection promise and reminder reject|collection task records|scheduled collection promise|HayHashvapah payment fulfills|bank transaction import reconciles|collection list query filters reject|CRM quote creation and release approval reject" test/api.test.js`, 7 pass, 0 fail, 0 cancelled); read-only subagent review pass (no blocking findings; residual pending-docs note addressed, broad malformed-path status contract unchanged by design); full `npm test` pass (504 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-crm-collection-path-id-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Current CRM quote approval path id guard checkpoint: `db51267` (`Harden CRM quote approval path ids`), pushed on `codex/suite-dashboard-route-normalization` (blocks missing CRM app entitlement and unsafe CRM quote release approval path IDs before quote lookup, workflow approval, suite-event, or audit work).
- Latest CRM quote approval path id guard verification from `~/dev/A1-Suite-Local`: `node --check server/app.js && node --check test/api.test.js` pass; focused CRM quote approval path/malformed metadata regression pass (`CRM quote creation and release approval reject malformed metadata before persistence`, 1 pass, including unsafe/overlong/encoded quote path IDs and disabled CRM app entitlement); broader CRM quote/lead/forecast guard suite pass (`node --test --test-name-pattern "draft CRM quote|CRM quote creation|public quote acceptance marks deal won|public quote endpoint exposes|CRM list query filters reject|CRM lead conversion rejects|CRM deal forecast rejects" test/api.test.js`, 7 pass, 0 fail, 0 cancelled); read-only subagent review pass after docs status-contract warning was addressed; full `npm test` pass (504 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-crm-quote-approval-path-id-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Current CRM deal forecast path id guard checkpoint: `812e9a1` (`Harden CRM deal forecast path ids`), pushed on `codex/suite-dashboard-route-normalization` (blocks unsafe CRM deal forecast path IDs before deal lookup, forecast row upsert, suite-event, or audit work).
- Latest CRM deal forecast path id guard verification from `~/dev/A1-Suite-Local`: `node --check server/app.js && node --check test/api.test.js` pass; `git diff --check` pass; focused CRM deal forecast path/malformed metadata regression pass (`CRM deal forecast rejects malformed metadata before persistence`, 1 pass); read-only subagent review pass (code/security, no blocking findings; residual transactionality/status-code-contract notes are non-blocking); full `npm test` pass (504 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-crm-deal-forecast-path-id-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Current CRM lead conversion path id guard checkpoint: `f479a74` (`Tighten CRM lead conversion overlong id guard`), pushed on `codex/suite-dashboard-route-normalization` (blocks unsafe CRM lead conversion path IDs before lead lookup, customer/deal/profile creation, conversion activity, suite-event, or audit work).
- Latest CRM lead conversion path id guard verification from `~/dev/A1-Suite-Local`: `node --check server/app.js && node --check test/api.test.js` pass; focused CRM conversion path/malformed metadata regression pass (`CRM lead conversion rejects malformed metadata before persistence`, 1 pass, including encoded malformed path IDs); broader CRM lead/forecast guard suite pass (`node --test --test-name-pattern "salesperson can capture|CRM lead API rejects|CRM lead conversion rejects|auditor cannot create CRM leads|forecast category|CRM deal forecast rejects|support cannot update CRM deal forecasts" test/api.test.js`, 7 pass, 0 fail, 0 cancelled); read-only subagent review warning addressed (encoded malformed path coverage restored; docs conflicts resolved); full `npm test` pass (504 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-crm-lead-conversion-path-id-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Current OpenRouter/Open Notebook AI merge checkpoint: `e930599` (`Merge current route-normalization base into AI branch`), on `feat/suite-consume-a1-ai` (merges the verified AI core refactor with `codex/suite-dashboard-route-normalization` so the open AI PR is no longer blocked by base-branch conflicts).
- Latest OpenRouter/Open Notebook AI merge verification from `~/dev/A1-Suite-Local-or`: AI syntax checks pass (`node --check server/app.js server/config.js server/aiProvider.js server/openNotebook.js server/copilot.js server/settingsStore.js test/api.test.js test/ai-config.test.js test/ai-provider.test.js test/settings-store.test.js test/open-notebook.test.js test/copilot.test.js test/ai-endpoints.test.js test/copilot-supplemental.test.js`); targeted AI/OpenRouter/Open Notebook suite pass (`node --test test/ai-config.test.js test/ai-provider.test.js test/settings-store.test.js test/open-notebook.test.js test/copilot.test.js test/ai-endpoints.test.js test/copilot-supplemental.test.js`, 52 pass, 0 fail, 0 cancelled); focused clinic next-recurring-ongoing-renewal HayHashvapah draft invoice packet regression pass (`accountant can record next recurring ongoing renewal HayHashvapah draft invoice packet after owner executes finance approval`, 1 pass); dashboard/sidebar route proof plus route/tax/VAT verification pass (`node --test test/suite-dashboard-sidebar-openability.test.mjs test/suite-routes.test.mjs test/tax-rate-versioning.test.js test/vat-rate-versioning.test.js`, 15 pass, 0 fail, 0 cancelled); read-only subagent merge review pass (no findings); full `node --test test/api.test.js` pass (231 pass, 0 fail, 0 cancelled); full `npm test` pass (504 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-ai-merge-current-base-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Current OpenRouter/Open Notebook AI core refactor checkpoint: `4bf4760` (`refactor: Suite consumes shared @a1/ai core (vendored) — no behavior change`), pushed on `feat/suite-consume-a1-ai` and `feat/openrouter-opennotebook-ai` (the suite consumes a vendored `server/vendor/a1-ai` core through thin local adapters for model catalog, model policy, settings storage, supplemental-source policy, and Open Notebook search).
- Latest OpenRouter/Open Notebook AI core refactor verification from `~/dev/A1-Suite-Local-or`: AI syntax checks pass (`node --check server/config.js server/aiProvider.js server/openNotebook.js server/copilot.js server/settingsStore.js server/app.js` plus AI test files); targeted AI/OpenRouter/Open Notebook suite pass (`node --test test/ai-config.test.js test/ai-provider.test.js test/settings-store.test.js test/open-notebook.test.js test/copilot.test.js test/ai-endpoints.test.js test/copilot-supplemental.test.js`, 52 pass, 0 fail, 0 cancelled); read-only subagent code review pass (no findings); standalone `node --test test/api.test.js` pass (231 pass, 0 fail, 0 cancelled); full `npm test` pass (504 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-or-ai-refactor-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Current clinic next recurring ongoing renewal closeout payment collection id guard checkpoint: `fae455c` (`Harden clinic next recurring closeout ids`), pushed on `codex/suite-dashboard-route-normalization` (validates clinic/wellness next-recurring-ongoing-renewal HayHashvapah payment-collection packet path IDs before next-recurring-ongoing-renewal closeout packet creation).
- Latest clinic next recurring ongoing renewal closeout payment collection id guard verification from `~/dev/A1-Suite-Local`: `node --check server/app.js` pass; `node --check test/api.test.js` pass; `git diff --check` pass; focused clinic next-recurring-ongoing-renewal closeout packet regression (`owner can close next recurring ongoing renewal cycle and schedule following recurring ongoing task after payment collection`) pass (1 target test across 48 files); read-only subagent review pass (code/security, no findings); full `npm test` pass (465 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-clinic-next-recurring-ongoing-renewal-closeout-payment-collection-id-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Current clinic next recurring ongoing renewal payment collection posting packet id guard checkpoint: `c44f095` (`Harden clinic next recurring payment ids`), pushed on `codex/suite-dashboard-route-normalization` (validates clinic/wellness next-recurring-ongoing-renewal official-invoice posting packet path IDs before next-recurring-ongoing-renewal HayHashvapah payment-collection packet creation).
- Latest clinic next recurring ongoing renewal payment collection posting packet id guard verification from `~/dev/A1-Suite-Local`: `node --check server/app.js` pass; `node --check test/api.test.js` pass; `git diff --check` pass; focused clinic next-recurring-ongoing-renewal payment collection packet regression (`accountant can record next recurring ongoing renewal payment collection packet after HayHashvapah receipt`) pass (1 target test across 48 files); read-only subagent review pass (code/security, no findings); full `npm test` pass (465 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-clinic-next-recurring-ongoing-renewal-payment-collection-posting-packet-id-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Current clinic next recurring ongoing renewal official invoice draft packet id guard checkpoint: `7f985d4` (`Harden clinic next recurring posting ids`), pushed on `codex/suite-dashboard-route-normalization` (validates clinic/wellness next-recurring-ongoing-renewal HayHashvapah draft invoice packet path IDs before next-recurring-ongoing-renewal official invoice posting packet creation).
- Latest clinic next recurring ongoing renewal official invoice draft packet id guard verification from `~/dev/A1-Suite-Local`: `node --check server/app.js` pass; `node --check test/api.test.js` pass; `git diff --check` pass; focused clinic next-recurring-ongoing-renewal official invoice posting packet regression (`accountant can record next recurring ongoing renewal official HayHashvapah invoice posting packet from next recurring draft`) pass (1 target test across 48 files); read-only subagent review pass (code/security, no findings); full `npm test` pass (465 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-clinic-next-recurring-ongoing-renewal-official-invoice-draft-packet-id-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Current clinic next recurring ongoing renewal draft acceptance handoff id guard checkpoint: `de23dae` (`Harden clinic next recurring draft ids`), pushed on `codex/suite-dashboard-route-normalization` (validates clinic/wellness next-recurring-ongoing-renewal acceptance handoff path IDs before next-recurring-ongoing-renewal HayHashvapah draft invoice packet creation).
- Latest clinic next recurring ongoing renewal draft acceptance handoff id guard verification from `~/dev/A1-Suite-Local`: `node --check server/app.js` pass; `node --check test/api.test.js` pass; `git diff --check` pass; focused clinic next-recurring-ongoing-renewal HayHashvapah draft invoice packet regression (`accountant can record next recurring ongoing renewal HayHashvapah draft invoice packet after owner executes finance approval`) pass (1 target test across 48 files); read-only subagent review pass (code/security, no findings); full `npm test` pass (465 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-clinic-next-recurring-ongoing-renewal-draft-acceptance-handoff-id-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Current clinic next recurring ongoing renewal acceptance release id guard checkpoint: `9990ab3` (`Harden clinic next recurring acceptance ids`), pushed on `codex/suite-dashboard-route-normalization` (validates clinic/wellness next-recurring-ongoing-renewal quote release packet path IDs before next-recurring-ongoing-renewal quote acceptance handoff creation).
- Latest clinic next recurring ongoing renewal acceptance release id guard verification from `~/dev/A1-Suite-Local`: `node --check server/app.js` pass; `node --check test/api.test.js` pass; `git diff --check` pass; focused clinic next-recurring-ongoing-renewal acceptance handoff regression (`accountant can hand accepted next recurring ongoing renewal quote into HayHashvapah invoice approval`) pass (1 target test across 48 files); read-only subagent review pass (code/security, no findings); full `npm test` pass (465 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-clinic-next-recurring-ongoing-renewal-acceptance-release-id-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Current clinic next recurring ongoing renewal quote release handoff id guard checkpoint: `7ef3236` (`Harden clinic next recurring release ids`), pushed on `codex/suite-dashboard-route-normalization` (validates clinic/wellness next-recurring-ongoing-renewal quote handoff path IDs before next-recurring-ongoing-renewal quote release packet creation).
- Latest clinic next recurring ongoing renewal quote release handoff id guard verification from `~/dev/A1-Suite-Local`: `node --check server/app.js` pass; `node --check test/api.test.js` pass; `git diff --check` pass; focused clinic next-recurring-ongoing-renewal quote release packet regression (`sales can create clinic next recurring ongoing renewal quote release packet after workflow executes`) pass (1 target test across 48 files); read-only subagent review pass (no findings); full `node --test test/api.test.js` pass (231 pass, 0 fail, 0 cancelled); dashboard/sidebar route proof plus route/tax/VAT verification pass (`node --test test/suite-dashboard-sidebar-openability.test.mjs test/suite-routes.test.mjs test/tax-rate-versioning.test.js test/vat-rate-versioning.test.js`, 15 pass, 0 fail, 0 cancelled); full `npm test` pass (465 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-clinic-next-recurring-ongoing-renewal-quote-release-handoff-id-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Current clinic next recurring ongoing renewal quote subsequent ongoing closeout id guard checkpoint: `844d481` (`Harden clinic next recurring quote closeout ids`), pushed on `codex/suite-dashboard-route-normalization` (validates clinic/wellness subsequent-ongoing-renewal closeout packet path IDs before next-recurring-ongoing-renewal quote handoff creation).
- Latest clinic next recurring ongoing renewal quote subsequent ongoing closeout id guard verification from `~/dev/A1-Suite-Local`: `node --check server/app.js` pass; `node --check test/api.test.js` pass; `git diff --check` pass; focused clinic next-recurring-ongoing-renewal quote handoff regression (`sales can create clinic next recurring ongoing renewal quote handoff from subsequent ongoing closeout`) pass (1 target test across 48 files); read-only subagent review pass (no findings); full `node --test test/api.test.js` pass (231 pass, 0 fail, 0 cancelled); dashboard/sidebar route proof plus route/tax/VAT verification pass (`node --test test/suite-dashboard-sidebar-openability.test.mjs test/suite-routes.test.mjs test/tax-rate-versioning.test.js test/vat-rate-versioning.test.js`, 15 pass, 0 fail, 0 cancelled); full `npm test` pass (465 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-clinic-next-recurring-ongoing-renewal-quote-closeout-id-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Current clinic subsequent ongoing renewal closeout payment collection id guard checkpoint: `840f291` (`Harden clinic subsequent ongoing closeout ids`), pushed on `codex/suite-dashboard-route-normalization` (validates clinic/wellness subsequent-ongoing-renewal payment-collection packet path IDs before subsequent-ongoing-renewal closeout packet creation).
- Latest clinic subsequent ongoing renewal closeout payment collection id guard verification from `~/dev/A1-Suite-Local`: `node --check server/app.js` pass; `node --check test/api.test.js` pass; `git diff --check` pass; focused clinic subsequent-ongoing-renewal closeout packet regression (`owner can close subsequent ongoing renewal cycle and schedule next recurring ongoing task after payment collection`) pass (1 target test across 48 files); read-only subagent review pass (no findings); full `npm test` pass (465 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-clinic-subsequent-ongoing-renewal-payment-collection-posting-packet-id-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Current clinic subsequent ongoing renewal payment collection posting packet id guard checkpoint: `d1f9326` (`Harden clinic subsequent ongoing payment ids`), pushed on `codex/suite-dashboard-route-normalization` (validates clinic/wellness subsequent-ongoing-renewal official-invoice posting packet path IDs before subsequent-ongoing-renewal HayHashvapah payment-collection packet creation).
- Latest clinic subsequent ongoing renewal payment collection posting packet id guard verification from `~/dev/A1-Suite-Local`: `node --check server/app.js` pass; `node --check test/api.test.js` pass; `git diff --check` pass; focused clinic subsequent-ongoing-renewal payment collection packet regression (`accountant can record subsequent ongoing renewal payment collection packet after HayHashvapah receipt`) pass (1 target test across 48 files); read-only subagent review pass (no findings); full `node --test test/api.test.js` pass (231 pass, 0 fail, 0 cancelled); dashboard/sidebar route proof plus route/tax/VAT verification pass (`node --test test/suite-dashboard-sidebar-openability.test.mjs test/suite-routes.test.mjs test/tax-rate-versioning.test.js test/vat-rate-versioning.test.js`, 15 pass, 0 fail, 0 cancelled) after restoring the missing Playwright Chromium headless shell cache; full `npm test` pass (465 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-clinic-subsequent-ongoing-renewal-payment-collection-posting-packet-id-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Current clinic subsequent ongoing renewal official invoice draft packet id guard checkpoint: `d46cff3` (`Harden clinic subsequent ongoing posting ids`), pushed on `codex/suite-dashboard-route-normalization` (validates clinic/wellness subsequent-ongoing-renewal HayHashvapah draft-invoice packet path IDs before subsequent-ongoing-renewal official-invoice posting packet creation).
- Latest clinic subsequent ongoing renewal official invoice draft packet id guard verification from `~/dev/A1-Suite-Local`: `node --check server/app.js` pass; `node --check test/api.test.js` pass; `git diff --check` pass; focused clinic subsequent-ongoing-renewal official invoice posting packet regression (`accountant can record subsequent ongoing renewal official HayHashvapah invoice posting packet from subsequent ongoing draft`) pass (1 target test across 48 files); read-only subagent review pass (no findings); full `node --test test/api.test.js` pass (231 pass, 0 fail, 0 cancelled); sidebar/route/tax/VAT verification pass (`node --test test/suite-dashboard-sidebar-openability.test.mjs test/suite-routes.test.mjs test/tax-rate-versioning.test.js test/vat-rate-versioning.test.js`, 15 pass, 0 fail, 0 cancelled); full `npm test` was attempted twice but interrupted by external signals after the Slice 310 path had passed, so no full-suite pass is claimed for this checkpoint; `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-clinic-subsequent-ongoing-renewal-official-invoice-draft-packet-id-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Current clinic subsequent ongoing renewal draft acceptance handoff id guard checkpoint: `204f7c3` (`Harden clinic subsequent ongoing draft ids`), pushed on `codex/suite-dashboard-route-normalization` (validates clinic/wellness subsequent-ongoing-renewal acceptance handoff path IDs before subsequent-ongoing-renewal HayHashvapah draft-invoice packet creation).
- Latest clinic subsequent ongoing renewal draft acceptance handoff id guard verification from `~/dev/A1-Suite-Local`: `node --check server/app.js` pass; `node --check test/api.test.js` pass; `git diff --check` pass; focused clinic subsequent-ongoing-renewal draft invoice packet regression (`accountant can record subsequent ongoing renewal HayHashvapah draft invoice packet after owner executes finance approval`) pass (1 target test across 48 files); dashboard sidebar openability regression pass after installing the missing Playwright Chromium headless shell (`dashboard sidebar opens every assigned app for all seeded roles`, 1 pass); interrupted route/tax/VAT tail rerun pass (14 pass, 0 fail); full `npm test` pass (465 pass, 0 fail, 0 cancelled); `npm run build:ui` pass with the existing Vite large-chunk warning; `ARMOSPHERA_ONE_DB=/tmp/a1-suite-clinic-subsequent-ongoing-renewal-draft-acceptance-handoff-id-guard-smoke.sqlite ARMOSPHERA_ONE_ALLOW_EGRESS=0 npm run smoke` pass (`smoke ok: Armosphera Demo Clinic, apps=10, kpis=4`).
- Current clinic subsequent ongoing renewal acceptance release id guard checkpoint: `99827ca` (`Harden clinic subsequent ongoing acceptance ids`), pushed on `codex/suite-dashboard-route-normalization` (validates clinic/wellness subsequent-ongoing-renewal quote release packet path IDs before subsequent-ongoing-renewal quote acceptance handoff creation).


## Provenance

- **Source path:** `/Users/samvelstepanyan/dev/A1-ERP-HY/HANDOFF.md` (first 400 lines extracted)
- **Source commit SHA:** `50f5f44d632f8a3112ae5579060b768f0028c5da`
- **Source file blob SHA1:** `603fcf367b9033cf5ad90e04f6a4b035d22b1e83` (full HANDOFF.md is 272631 bytes)
- **Extracted slice SHA1 (first 400 lines):** `fc8e8b163ad0a9edb9aae9a7675f5d4cf417888d` (128372 bytes)
- **Mirror date:** 2026-06-16
- **Worktree:** `/Users/samvelstepanyan/dev/SBOS-A1-ERP/.claude/worktrees/seed-from-a1-erp-hy`
- **Bytes (mirrored body, pre-provenance):** 128372
- **Note:** This is an *extract* (not a verbatim full copy) of `HANDOFF.md`. For the complete 272631-byte document, consult the A1-ERP-HY source at the path above.
